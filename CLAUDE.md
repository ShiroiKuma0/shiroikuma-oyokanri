# CLAUDE.md — 白い熊 応用管理 (`shiroikuma.oyokanri`)

This file is loaded automatically by Claude Code. It captures the fork's facts,
build pipeline, and operating conventions so a fresh session can pick up
exactly where the previous one left off. The full per-commit changelog and
engineering lessons live in **`.claude/skills/appmanager-fork/SKILL.md`** —
consult that for the "why" behind anything in here.

## Project

- Fork of [AppManager](https://github.com/MuntashirAkon/AppManager) (upstream base **4.1.1**, minSdk 21, AGP 8.13.2).
- `applicationId`: `shiroikuma.oyokanri` (installs side-by-side with the official build; renamed from the former `shiroikuma.appmanager`, so it is a fresh install rather than an update).
- Java package (unchanged from upstream): `io.github.muntashirakon.AppManager`.
- Display label: 白い熊 応用管理.
- Remote: `origin` → ShiroiKuma0/shiroikuma-oyokanri (formerly ShiroiKuma0/appmanager); `upstream` → MuntashirAkon/AppManager.
- Branch: **`custom`** — all fork work lives here; pushes go to `origin/custom`. `master` mirrors upstream.
- **Upstream tracking: `git`** (白い熊, 2026-08-06) — `custom` is rebased onto upstream **master commits**, not onto release tags, so the fork versionName pins the upstream base: `<upstream>+<base commit date>.<HH-MM>.g<sha8>+<BUILD_NUMBER, 3 digits>`, e.g. `4.1.0+2026-06-29.21-57.gfc1e7007+089` (the `HH-MM` and the `+` grouping arrived 2026-08-12; this repo was already rendering the timestamp in UTC). See the global **`git-versioning`** skill for the rationale and the exact format, and the versioning rules under *Build & deploy pipeline* below for what this repo must NOT do with the counter.

## Target device & environment

- Huawei Mate XT (tri-fold, Android 13, non-rooted, all-files access).
- ABI shipped: `arm64-v8a` only.
- Dev machine: Tuxedo OS Prague (TZ **Europe/Prague**).
- JDK: `/usr/lib/jvm/java-21-openjdk-amd64` (Japanese locale, so `javac` prints `ノート:` instead of `Note:`).
- Android SDK: `~/android-sdk`. Each build writes `local.properties` with `sdk.dir=$ANDROID_HOME`.
- Keystore: `~/.android-keystores/appmanager-custom.jks`
  - alias: `appmanager`
  - keystore + key password: `appmanager123`

## Build & deploy pipeline

**Always build after changes.** Whenever you finish a set of working-tree edits (Java/XML/resources), run the full pipeline below to completion — bump, assemble, sign, verify, copy the signed APK to `~/tmp/` — without waiting for the user to say "Build". Treat a task as unfinished until it has produced a fresh signed APK on disk. If the build fails, stop and surface the error rather than reporting the change as done.

**Deliver automatically after every build — never ask.** Standing instruction from 白い熊 (2026-08-06), replacing the former "always ask via `AskUserQuestion`" rule: follow the global **`/after-build`** skill and its delivery chain, and do not put a question between the build and the delivery. After a successful build, walk the chain yourself — `adb devices` (**unsandboxed**, per `/adb-check`) → `adb connect 192.168.1.73:5555` → `adb connect skhw:15555` → `scp` to `skhw:~/tmp/` — and deliver **exactly once**, to the first target that answers. Those are all the **same machine**, so never deliver by two paths. Never ask "how should I deliver?" and never ask "is the phone connected?" — the chain answers that itself. Any wireless adb session opened for the batch gets `adb disconnect`ed at the end of it (check `ps` for a live `scrcpy` first). Then say plainly what landed and where.

Output APK is named `shiroikuma-oyokanri_$(tools/fork-version.sh)_arm64-v8a.apk`, e.g. `shiroikuma-oyokanri_4.1.0.2026-06-29.gfc1e7007+089_arm64-v8a.apk`. Always copy the signed APK to `~/tmp/` before pushing to the device, so a record stays on disk.

**The versionName pins the upstream commit we are rebased on (+89).** `custom` follows upstream **master**, whose versionName literal (`4.1.0`) has not moved since 2026-06-29 and will not move until upstream releases — so `4.1.0+088` said nothing whatever about how current the fork was. The name is now `<customBaseVersionName>+<base commit date>.<HH-MM>.g<sha8>+<NNN>`, where the sha is `git merge-base HEAD master` — **the upstream commit our patches sit on**, never our own HEAD (that is what `+N` and the release tag already say) and never `master`'s tip (which overstates the base whenever master is fast-forwarded but `custom` not yet rebased). The timestamp is that commit's **committer date and time in UTC** — never build time, or two builds on one upstream base would disagree — and exists only so the names sort: a bare sha is random text and buries the newest APK mid-list, and a bare date ties whenever two syncs land on one day, handing the ordering straight back to that sha (hence `HH-MM`, 白い熊 2026-08-12). **Two halves that must stay in step**: `upstreamBasePin()` in `app/build.gradle` (Gradle, via `providers.exec` — a plain `ProcessBuilder`/`String.execute()` is a configuration-cache failure, which is why `buildTime()` already does it this way) and **`tools/fork-version.sh`** (shell, the single source of truth for the pipeline and for `tools/bump-build.sh`'s printed line). Both **degrade rather than fail**: no `master` ⇒ no pin, git present but the timestamp unreadable ⇒ `+g<sha>` alone. A missing pin must never cost a build.

**LANDMINE — do NOT reset `customBuildNumber` on an upstream sync in this repo.** The `git-versioning` skill says the counter resets to 1 on each sync; that holds where the upstream commit feeds `versionCode`. Here it does not — `versionCode = customBaseVersionCode * 10000 + customBuildNumber`, and `customBaseVersionCode` (450) only moves when upstream cuts a **release**. Resetting the counter after an ordinary master rebase would therefore drive the versionCode **backwards** and Android would refuse the install as a downgrade. Reset it **only** in the same edit that raises `customBaseVersionCode`; every other rebase just keeps counting, and the pin is what records that upstream moved.

**The build counter is zero-padded to three digits — in the filename AND in `versionName` (+63).** The `/after-build` convention pads the filename so `~/tmp` sorts in build order; `app/build.gradle` must pad too, or the two disagree in the place that matters. Before +63 the file read `+062` while the **manifest** read `+62`, which is what Android and every installer actually show — 白い熊 caught it in shiroikuma-universalinstaller's install dialog. `versionName "${customBaseVersionName}${upstreamPin}+${String.format('%03d', customBuildNumber)}"`, and `tools/bump-build.sh` prints the same by delegating to `tools/fork-version.sh` (it used to render its own `printf '%s+%03d\n'`, which is exactly how a second copy of the format drifts). `versionCode` stays plain integer arithmetic, so upgrade ordering is untouched. Note the `publish-version` skill derives its git tag from the version name, so tags carry the padding from +063 on; earlier published tags (`4.1.0+55`) are **never** retagged.

```bash
# from repo root
export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
export PATH="$JAVA_HOME/bin:$PATH"
export ANDROID_HOME=$HOME/android-sdk

tools/bump-build.sh                            # increments customBuildNumber
echo "sdk.dir=$ANDROID_HOME" > local.properties

./gradlew clean
# Filter benign output on BOTH streams; preserve the exit code with pipefail.
set -o pipefail
./gradlew :app:assembleRelease \
    2> >(grep -vE 'ノート:|Note:|\[CXX5304\]' >&2) \
  | grep -vE 'ノート:|Note:|\[CXX5304\]'

UNSIGNED_APK=$(find app/build/outputs/apk/release -name '*-unsigned.apk' | head -1)

# HARD GATE: the embedded server JARs must be present AND non-empty, or ADB mode
# is dead on arrival (see "Server JARs" note below). Refuse to sign otherwise.
for j in assets/am.jar assets/main.jar; do
    sz=$(unzip -p "$UNSIGNED_APK" "$j" 2>/dev/null | wc -c)
    [ "$sz" -gt 0 ] || { echo "FATAL: $j is empty/missing in APK — refusing to ship"; exit 1; }
done

zipalign -p -f 4 "$UNSIGNED_APK" /tmp/am-aligned.apk
apksigner sign --ks ~/.android-keystores/appmanager-custom.jks \
    --ks-key-alias appmanager \
    --ks-pass pass:appmanager123 \
    --key-pass pass:appmanager123 \
    --out /tmp/am-signed.apk /tmp/am-aligned.apk
apksigner verify --verbose /tmp/am-signed.apk 2>&1 | grep -v 'not protected by signature'

# Base version + upstream-base pin + zero-padded counter, from the one place that renders it.
# Never re-derive this from gradle.properties by hand — the pin would be dropped and the
# filename would stop matching the manifest.
apk_name="shiroikuma-oyokanri_$(tools/fork-version.sh)_arm64-v8a.apk"
cp /tmp/am-signed.apk ~/tmp/"$apk_name"

# Deliver straight away — first target that answers, exactly once (see the
# delivery rule above). adb ALWAYS runs unsandboxed.
adb push /tmp/am-signed.apk "/sdcard/tmp/$apk_name"    # or: scp ~/tmp/"$apk_name" skhw:~/tmp/
```

Filter notes:
- `ノート:` / `Note:` are mandatory `javac` summary notes for deprecation/unchecked usage; `-Xlint:none` and `-nowarn` do not suppress them.
- `[CXX5304]` is a benign SDK-XML version-skew message from the NDK pipeline. Brackets must be escaped (`\[CXX5304\]`) for `grep -E`.

**Server JARs (`assets/am.jar` + `main.jar`) — the ADB-mode landmine.** Both are
gitignored and regenerated every build by `:server:create<Variant>ServerJars`
(server/build.gradle), which `d8`-compiles them straight into `app/src/main/assets/`.
`main.jar` is the bootstrap the ADB shell runs to launch AppManager's server; an
empty/missing one makes **ADB mode hang on init then fall back to no-root** (build
+79 shipped a **0-byte main.jar** this way and looked like a code regression — it
was a build race). Two structural guards now prevent it, plus the gate above:
1. **Ordering** — `app/build.gradle` makes `merge<Variant>Assets dependsOn
   :server:create<Variant>ServerJars` via a plain `tasks.matching` edge (the old
   `applicationVariants.mergeAssetsProvider` edge was unreliable under the
   configuration cache — that's how +79 slipped through).
2. **Atomic writes** — server/build.gradle `d8`s each JAR into a **per-variant**
   `server/build/serverjars-tmp/<buildType>/` dir (temp name must keep the `.jar`
   suffix — d8 rejects others; per-variant because release+debug jar tasks run in
   parallel and share nothing), then `Files.move`s it into `assets/`, so a racing
   consumer sees a whole JAR or none, never a partial. Diagnose a suspected recurrence
   with `unzip -p <apk> assets/main.jar | wc -c` (compare a known-good vs bad APK).

## Push workflow

When the user says **"Push"**:
1. `git add -A` (include new files).
2. Show `git status` for the user to review.
3. `git commit -m "<concise imperative title>" -m "<multi-line body describing what changed and why>"`.
4. `git push origin custom` — **never force-push**.
5. Report the resulting commit hash and a one-line summary.

## Translation workflow

The user supplies an English label plus its Japanese translation and where they see it. Procedure:
1. Find the matching string-resource name in `app/src/main/res/values/strings.xml` (e.g. "Frozen apps" → `name="filter_frozen_apps"`).
2. Add `<string name="…">…</string>` to `app/src/main/res/values-ja/strings.xml` under the marker `<!-- Fork: user-supplied Japanese translations -->`.
3. The translation applies wherever that resource is referenced, including the toolbar (via `MainToolbarPrefs.titleForKey`).

## Protected profile (`必要`)

Any app that is a member of an apps profile named **`必要`** is hard-blocked from being frozen and uninstalled. Enforcement sits at the two low-level chokepoints, so no UI path (single-app, batch, profile application, future code) can bypass it.

| Chokepoint | Behaviour |
|---|---|
| `FreezeUtils.freeze(pkg, userId, type)` | Throws `RemoteException` for protected packages. The 2-arg `freeze` delegates to this; all freeze methods (disable/suspend/hide) run after the guard. |
| `PackageInstallerCompat.uninstall(pkg, userId, keepData)` | Returns `false` at the very top, before any work or accessibility dialog. |
| `profiles/ProtectedAppsProfile.java` | Helper. `getProtectedPackages()` unions `packages` from every apps-profile named `必要`. 3 s TTL cache. **Fails open** (a stuck block is worse than briefly missing protection). Call `invalidate()` after profile changes. |

Concrete refusal messages fire at four entry points — the main-list freeze icon, app-details freeze, app-details uninstall, and batch freeze/uninstall — using `R.string.protected_profile_block` (single, `%s` = label) and `R.string.protected_profile_block_multiple` (list, `%s` = names joined with `、`).

## Key files & chokepoints

| Concern | File |
|---|---|
| Launcher resumes where you were | `main/SplashActivity.java` is a **trampoline**: it starts `MainActivity` and finishes, so the task it leaves is rooted at `MainActivity` and no longer contains it. Tapping the launcher icon while that task exists drops a **fresh** Splash on top, which used to stack a **fresh** `MainActivity` over whatever screen (and tab) the user was on — the task was never damaged, just buried. `onCreate` now computes `mLaunchedOntoExistingTask` = `!isTaskRoot() && ACTION_MAIN && CATEGORY_LAUNCHER` **first thing** (`getIntent()`/`isTaskRoot()` are valid from `attach()`, i.e. before `super.onCreate()`); if that and `Ops.isAuthenticated()`, it calls `super.onCreate` → `finish()` → `overridePendingTransition(0, 0)` and returns **before** the theme, `installSplashScreen` and `setContentView`, so nothing of ours is drawn and there is no flash. Both hand-off sites go through `handOff()`, which finishes instead of starting `MainActivity` in that case — including after the auth/ADB flow, which is the path taken when EMUI killed the process and the task was restored around us (privileges are up before the restored screen resumes). **`super.onCreate()` must still be called or the framework throws `SuperNotCalledException`.** |
|---|---|
| **A lost privileged session is reclaimed, and shown** (白い熊, +97) | `settings/PrivilegeWatchdog.java`. Every privileged call runs through the `IAMService` binder, and a Shizuku server restart (an update of 白い熊 雫, a stopped server, a killed user-service process) takes it with it. The **entire** reaction used to be `ServiceConnectionWrapper.onServiceDisconnected` nulling its field: `LocalServices.alive()` then answered false, `Runner.getInstance()` quietly returned the **no-root** shell and `ProxyBinder` fell back to local binders — nothing threw, nothing was shown, and privileged operations silently did nothing. `Ops.init` is reached only from `SecurityAndOpsViewModel` (the splash gate) and `MainPreferencesViewModel` (the mode picker), so the only cure was restarting the app. **Reclaiming is free when it is possible at all**: authorisation lives in the *server's* own persisted list and survives the restart, and the returning server **pushes a fresh binder** into our `ShizukuProvider` → `Shizuku.onBinderReceived`. Both that and the death notice already existed and were simply never listened for — no `OnBinderDeadListener` anywhere, and the one `OnBinderReceivedListener` was the transient sticky one inside `ShizukuOps.awaitBinder`, removed in its own `finally`. The watchdog is the listener that stays, installed once from `AppManager.onCreate` (the server pushes whenever it likes, including while no activity exists). **The reclaim is `Ops.init(context, false)`, never a bind of our own** — that method is the one place that knows how each mode is entered, corrects the session flags from the uid the service actually came up as, and re-runs `SelfPermissions.init()`. **LANDMINE — a deliberate teardown is indistinguishable from a death.** A mode switch tears the old service down before binding the new one and fires the same `onServiceDisconnected`; every such teardown in the app happens inside `Ops.init` (`LocalServices.stopServices` has no other caller, `unbindServices` is only reached from `bindServices`), so `Ops.init` brackets itself with `noteInitStarted()`/`noteInitFinished()` and a drop seen inside the brackets is ours. No timer separates the two. **Nothing is alarming until something is actually lost**: `sHadPrivileges` latches the first time the services are seen alive, so an install that has always been no-root never sees a red bar. `MODE_AUTO` counts as "Shizuku is the chosen route" — auto-detection hunts Shizuku first and the fork deliberately leaves the preference on auto whenever a manager is installed. Automatic reclaims gate on `ShizukuOps.isReady()` so `Ops.init` finds authorisation already in place and **raises no prompt**; only a tap on the alarm passes `interactive=true`. UI = `@id/privilege_alarm`, a pinned red bar inside the `AppBarLayout` below the progress indicator (no scroll flags, so it cannot be scrolled away), wired in `MainActivity.setupPrivilegeAlarm` and refreshed from `onResume` for whatever happened while we were away. Colours are the Snooping page's "allowed" pair (`0xFF6E0B14` on `0xFFFFD9DC`), **not** the theme yellow, which here would read as ordinary furniture; the icon pulses only in `STATE_LOST` and the animator is cancelled in `onDestroy` (an INFINITE animator holds its target view). Add `PrivilegeWatchdog.onServiceLost()` to any new way the binder can drop. |
|---|---|
| **LANDMINE — a relative manifest name expands against `namespace`, NEVER against `applicationId`** (白い熊, +043) | AGP resolves `<activity-alias android:name=".intercept.BrowserInterceptor">` using the **`namespace`** (`io.github.muntashirakon.AppManager`), not the `applicationId` (`shiroikuma.oyokanri`). Upstream those two strings are the same, so upstream code may build a component name out of `BuildConfig.APPLICATION_ID` and be right; **in this fork it is always wrong**, and wrong in the most expensive way — `PackageManagerService.setEnabledSetting` throws `IllegalArgumentException: Component class … does not exist` for any app targeting P or later, so the call site dies rather than no-opping. `ActivityInterceptor.ALIAS_BROWSER` was exactly this: `FeatureController.modifyState(FEAT_INTERCEPTOR, …)` disabled the target activity, then threw on the alias **before** the `AppPref.set(PREF_ENABLED_FEATURES_INT, …)` at the bottom of the method, so unticking *Interceptor* in Settings → Appearance → *Enable/disable features* could neither finish nor persist and the Interceptor kept answering every `ACTION_VIEW` http/https intent in the system chooser. Fixed by spelling the namespace out, as the two sibling constants `AppDetailsActivity.ALIAS_APP_INFO` and `CodeEditorActivity.ALIAS_EDITOR` always did. **Rule: a `ComponentName` for one of OUR components is `new ComponentName(BuildConfig.APPLICATION_ID, "io.github.muntashirakon.AppManager.…")` — package from the applicationId, class from the namespace.** `BuildConfig.APPLICATION_ID` remains correct for intent-extra keys and authorities, which is most of its uses. |
| **A feature flag travels; its component's enabled state does not** (白い熊, +043) | `FeatureController` holds each feature in two places: the bit in `PREF_ENABLED_FEATURES_INT`, which lives in `shared_prefs/` and therefore rides in every settings export, and the component's enabled state, which lives in `system_server` and rides in nothing. `isEnabled()` **ANDs the two**, so when they disagree the feature disappears from our own UI (Labs, our open-with sheet) while the manifest entry keeps answering the **system**. They disagree on exactly the case this family is built for — a settings import onto a clean phone — and the `shiroikuma.appmanager` → `shiroikuma.oyokanri` rename did it to every install at once, since a renamed applicationId is a fresh install whose component states all start at *enabled*. `FeatureController.reassertDisabledComponents()` (called from `AppManager.onCreate` on a background thread — these are binder calls) re-asserts the **disables** the flags already record. **One-directional on purpose**: a flag that says *enabled* never re-enables a component, because a component disabled by some other route was disabled deliberately. Each feature is guarded separately so a name that does not resolve can never take the process down — which is precisely what the alias above did. `componentFor(key)` is the single mapping, shared with `isEnabled`. |
| Freeze chokepoint | `app/src/main/java/io/github/muntashirakon/AppManager/utils/FreezeUtils.java` |
| **A freeze is four gates, not one** (白い熊, +29) | `FREEZE_TOTAL` — force-stop → suspend → disable-user → **hide**, in that order, each best-effort, and the default freezing method. **Order is load-bearing**: hide last, because a hidden package is reported as *not installed* and the suspend/enabled-state calls then have nothing to act on. It is a **distinct method value** (`1 << 4`), not a mask of the others: the `FREEZE_*` constants are bit flags but every consumer compares them with `==`, and the value is the wire format of `FreezeRule` and of the per-app remembered method. Made the default by a second one-shot migration (`migrateDefaultFreezingMethodToTotal`, guard `PREF_FREEZE_TYPE_MIGRATED_TOTAL_BOOL`) firing only while the stored value is still exactly `FREEZE_ADV_SUSPEND` — same shape and same landmine as the +100 migration above it. **Measured 2026-09-10 on the phone (`HUAWEI GRL-LX9`, `SDK_INT 31`), confirmed by 白い熊 on the second phone; every claim below is a measurement, not a reading of AOSP.** **(1) The shell holds no `MANAGE_USERS`** — `pm hide` answers `SecurityException: Neither user 2000 nor current process has android.permission.MANAGE_USERS`, while `SUSPEND_APPS`, `CHANGE_COMPONENT_ENABLED_STATE`, `FORCE_STOP_PACKAGES` and `DELETE_PACKAGES` are all granted. So `PackageManagerCompat.hidePackage` **throws** here and the *Hide* freezing method had been silently degrading to *Disable* for as long as this fork has run on this phone. Hiding is reachable **only** through 雫's `DELEGATION_PACKAGE_ACCESS` (`DevicePolicyBridge.setHidden`), the same scope that carries `setPackagesSuspended` — so a phone whose Snooping suspend switch works can hide, and one without the delegation can do neither. **(2) `pm disable` (state 2) is refused outright**, and not only per component: at **package** level too — `SecurityException: Shell cannot change component state for <pkg>/null to 2`. Component level refuses **both** state 2 and state 3. The platform confines the shell to ENABLED/DEFAULT/DISABLED_USER on whole packages and to nothing at all on components (the +23 rule, now measured at package scope). **(3) Suspension is far stronger than its documentation** — it blocks the activity start (no process appears), the launcher path, and an **explicit broadcast to a resolvable receiver, even with `FLAG_INCLUDE_STOPPED_PACKAGES`**; and `pm suspend` on a *running* app kills the process by itself. **But it leaves `stopped=false`**, so the force-stop in Advanced suspend is **not** redundant: it is what sets the stopped flag. **(4) Disable-user is the resolution gate.** The cleanest discriminator is a content provider: suspended it answers `SecurityException: … not exported from UID …` (it resolved), disabled it answers `IllegalStateException: Could not find provider` (it does not exist). Suspension leaves every component resolvable; nothing else in the stack does. **(5) Two suspension slots.** The platform records suspension per *suspending package*: the shell path writes `suspendingPackage=com.android.shell`, the delegated path writes `android`. A shell `pm unsuspend` clears only the shell's slot and returns looking like success. `suspendBestEffort` therefore **prefers the admin slot** — it is the one `adb` cannot lift. **(6) A PERSISTENT system app keeps running through all four gates** (`com.huawei.hiview`: `flags=[ SYSTEM HAS_CODE PERSISTENT ]`, AMS `*PERS*`, process start time proving it was never killed — `ActivityManagerService.forceStopPackageLocked` deliberately spares persistent processes). The gates stop an app being *started*; they cannot evict a process AMS brought up at boot. **白い熊 自由作業盤 MIRRORS this gate set** (its `AppFreeze.freeze`/`thaw`, from its +086): it holds the same `DELEGATION_PACKAGE_ACCESS` and calls the same public SDK with a `null` admin, so it declined an automation contract from us and asked instead to be **told whenever the gate set changes** — a fifth gate, or hide dropped. Tell that repo before shipping such a change, or its thaw silently stops clearing everything ours applies. Its protected trio is the same three as `ProtectedAppsProfile.ALWAYS_PROTECTED`. |
| **LANDMINE — the FIRST render of the list comes from a different builder** (白い熊, +037) | `PackageUtils.getInstalledOrBackedUpApplicationsFromDb` builds the list the app opens with; `MainViewModel.getNewApplicationItem` rebuilds a row per package afterwards, off broadcasts and targeted re-reads. **Both must fill every live flag, or a new one is simply absent for the first ten seconds** and then appears — which reads as the app being slow to notice something it had in fact already read. The first builder set `isFrozen` from its bulk `getInstalledApplications` and stopped there, so a frozen row drew the *fallback* ice-blue snowflake with no rung colour, no shading and no badge: not level 2, **no level at all**, `isFrozen == true` with `freezeLevel == 0`. `isSuspendedApp` had the identical gap from +81 and was invisible only because suspension was rare. Both are filled there now, from the `ApplicationInfo` that builder already holds — free, and already carrying `MATCH_UNINSTALLED_PACKAGES \| MATCH_DISABLED_COMPONENTS`, which is what a hidden package needs to be seen at all. **Rule: any new per-row flag read from a live `ApplicationInfo` must be set in BOTH places**, and the fallback branches (no live info) must claim only what the DB actually knows — disabled, never a deeper rung. |
| **A freeze has a level, and the whole app says which** (白い熊, +034, badge reworked +035/+036) | `FreezeUtils.levelOf(ApplicationInfo)` = the **highest gate standing**, 0-4; `main/FreezeLevelBadge.java` turns that number into a colour and draws the badge; `ColorPrefs.FREEZE_LEVEL_1..4` + `FILM_LEVEL_2..4` are the ramp (defaults `theme_freeze_1..4` / `theme_film_freeze_2..4`, pale aqua → violet, all settable under *UI colors & fonts → Freeze levels*). Once each gate got its own switch, "frozen" stopped being one state and the row could no longer say which one you were looking at. Now: the main list's **film** is the rung's colour, the **snowflake** takes it too, and a **numbered ring sits BESIDE the snowflake at the same 26dp** (`@id/freeze_level_badge`, +036). It began overlapping the glyph's corner at 16dp, which cost the digit most of its size; beside it, it is simply legible. **It fits a 60dp icon column only because it can never share the line with the ✕**: that one is shown for a RUNNING app (`isInstalled && !isFrozen && !isStopped`), which is exactly freeze level 0, while the badge appears from level 1 up — check that invariant before adding anything else to that row. **The badge is a RING, never a disc** (白い熊, +035): hairline outline in the rung's colour, the digit in the same colour — the fork's own pill language, and the right way round for a mark drawn *on* the glyph, where a filled disc punched a solid coloured hole that read as damage. The fill is genuinely `TRANSPARENT` since +036; while it overlapped the glyph it had to be filled with the row's own film colour, or the snowflake's arm ran through the digit. 26dp with a 1.5dp hairline carries a 16sp digit. **Rung 1 is the fork's YELLOW**, not a cool tint: the badge sits on a *thawed* snowflake, and giving it a colour of its own made an unfrozen row look half-frozen. The ladder proper starts at rung 2, where the freezing starts. The 盗み見 Freeze box uses the **same builder**: each step lights in its own rung, the master switch and the box frame wear the deepest rung standing. **Level 1 is not a freeze** — `FreezeUtils.isFrozen` is false for a merely stopped package and anything that opens it clears the mark — so it has an accent and a badge but **no film**, and its row stays an ordinary row; `freezeFilmKey(1)` returns null for exactly that reason. **Expect many ① badges**: `FLAG_STOPPED` is also set for an app that has never been launched since install, not only for one you force-stopped. `ApplicationItem.freezeLevel` is filled from the same live `getPackageInfo` that fills `isFrozen`/`isSuspendedApp` (match flags mandatory, or a hidden package resolves to nothing and every gate reads false); the stale-cache branch claims `GATE_DISABLE` at most, since disabled is all the DB knows. `MainCardBinder` derives the level from the item, else from the `ApplicationInfo`, and goes through the same `FreezeLevelBadge` — the +30 rule says those surfaces share colour keys, and sharing the builder is strictly better. **Retired with it**: `FREEZE_SUSPENDED`, `FILM_FROZEN` and `FILM_SUSPENDED` no longer paint anything, so their settings rows and legend entries are gone (the keys stay — they are in exported prefs files); `FREEZE_FROZEN` survives only as the fallback for a surface that cannot work the rung out. The suspended row keeps its **padlock** — shape reads further than colour at 26dp — but takes the ladder's accent, since suspension is no longer the deepest thing on the list. |
| **LANDMINE — a privileged read on the main thread hangs the whole app after every install** (白い熊, +033) | `AppDetailsSnoopingFragment.onViewCreated` called `SnoopingEnforcer.canEnforce()` inline, from the original 盗み見 commit. That reaches `SelfPermissions.canModifyAppOpMode` → `Users.getSelfOrRemoteUid()` → **`LocalServices.getAmService()`, which is `synchronized`** on the service-connection wrapper. It returns in microseconds while the `:am` server is bound and **blocks for the whole bind** while it is not — and the bind holds that same monitor from another thread (`PrivilegeWatchdog.reclaim` → `Ops.init` → `LocalServices.bindServices` → `ServiceConnectionWrapper.startDaemon`, parked on a `CountDownLatch` for the daemon). **The two meet on every fresh install**: replacing the package kills the old privileged server, the watchdog reclaims it, and the reopen-last-screen feature (+146) opens this very page while that is in flight. Symptom is a **black window and then the app dies** — which reads as a crash and is an **ANR**: `Input dispatching timed out … Waited 10006ms for FocusEvent`. **Diagnosis method, because the obvious ones fail**: `logcat -b crash` shows nothing (an ANR is not an exception), `/data/anr/*` is `system:system 0600` so the shell cannot read it, and `debuggerd -j` answers `root is required`. **`adb bugreport` is the way in** — it carries *VM TRACES AT LAST ANR* (the stored file) and *VM TRACES JUST NOW* (every process, dumped live), which is how the blocked frame and the lock **holder** were both identified. Fixed by asking on a worker and filling the banner in when the answer arrives; the page is useful meanwhile because every capability row does its own privilege check. **The same shape is still live in two upstream tabs** — `AppDetailsComponentsFragment.onCreateMenu` (`SelfPermissions.canModifyAppComponentStates`) and `AppDetailsOverlaysFragment.onCreate`/`onViewCreated` (`checkSelfOrRemotePermission`) — untouched so far, and they will hang identically if one of those tabs is the reopened screen. **Rule: no `SelfPermissions`/`Users`/`LocalServices` call belongs in a lifecycle or menu callback.** |
| **A level is a bulk action, and it is the only record of how a phone is frozen** (白い熊, +038) | `FreezeUtils.setLevel(pkg, userId, level)` plus `BatchOpsManager.OP_FREEZE_LEVEL_1..4`, four pills on the selection pane (`MainToolbarPrefs` keys `freeze_level_1..4`, ring icons `ic_freeze_level_N`). A level means **gates 1‥N applied and every gate above released**, so level 4 is exactly `freezeTotal` and the lower rungs are it truncated. **Order is load-bearing twice**: release runs first and top-down (a hidden package is reported as not installed, so nothing below gate 4 can be read or written while it stands), then apply runs bottom-up for the `freezeTotal` reason. A gate already in the wanted state is skipped rather than re-written — `setGate` would happily re-issue it, and a force-stop of an already-stopped app is a real kill while a re-suspend rewrites the suspending-package slot. **Success is `levelOf` afterwards, never the calls returning**, which makes it a test of the *top* gate: a PERSISTENT process that survives the force-stop under a disable gate still reads level 3, and that is the truth. **Four ops rather than one carrying a level** because `BatchOpsService.getDesiredOpTitle` is handed the op and nothing else — the notification, the operation bar and the history would otherwise all say only "Freeze". **The per-app remembered method is deliberately NOT written** (the +032 rule: the method is what the snowflake does next). `freezeTargetForOp` returns null for these, so `applyBatchOpResult` falls to the targeted re-read — right, because an optimistic snap would have to guess whether each gate landed, and the hide gate genuinely varies per app. **Why it exists (白い熊, 2026-09-12): the level is the ONLY thing that records how this phone is frozen, and it is not persisted anywhere** — `levelOf` reads it live off `ApplicationInfo` on every bind, the gates persist nothing, and the settings export carries only `shared_prefs/` + `files/profiles/` + `files/fonts/` (the per-app remembered method lives in the Room `freeze_type` table and does not travel; an app backup's `FreezeRule` carries that method but `RestoreOp` only `storeFreezeMethod`s it and **never freezes anything**). So the way to reproduce a phone's freezing is a **profile per depth** — profiles DO travel, as `Category.PROFILES` — filter the list to the profile, select all, tap the rung. That is the workflow these four pills were asked for. **The single-app pane carries the same four rungs (白い熊, +039)**, in place of its Freeze pill (`freeze` stays an `AppPanePrefs` key, hidden, so it can be brought back): there the rung the app is standing on is drawn **filled** — `AppPaneBinder.isLit`, off `ApplicationItem.freezeLevel`, the same number the row's badge shows, so pill and badge cannot disagree — and **tapping the lit rung releases to rung zero**, the shelf's own rule that one pill is both the way in and the way out. That is why `setLevel` takes **0**: it is a release, so it skips the 必要 guard and the freeze-myself guard (the way back must always exist) and lifts every gate including the force-stop, which `unfreeze` deliberately leaves alone. The pane goes straight to `setLevel` on a worker rather than through the batch service, as `toggleFreeze` does — one app is instant and a protected app's refusal is worth showing where the tap was — and it re-broadcasts `sendPackageAltered` **on the failure path too**, since a partial walk still moved the app. The snowflake under the icon still thaws, unchanged. **Labels are "Freeze ①‥④" and the pill leads with the snowflake (白い熊, +040)** — the circled digit is the badge the row draws, so pill and badge read as one thing, and it is in the *text* rather than as the leading glyph so it is not shown twice. **The gate's own name stays** (白い熊, +041, after it was briefly dropped in +040): a rung that does not say what it does is a number and nothing else, and the word is how you pick the right one without opening the freeze box. It does read close to the Force-stop pill beside it. **They are not the same action** — identical only on an app at level 0. On a frozen app, Force-stop adds gate 1 and leaves the freeze standing, while rung 1 tears every gate above it down; and on an app already at rung 1 (very common — `FLAG_STOPPED` is set for anything never launched since install) the lit pill *releases* the stopped flag, i.e. the opposite. Force-stop therefore stays: it is "kill this now without changing how frozen it is", which no rung can express. |
| **The four gates are four switches** (白い熊, +032) | `FreezeUtils.GATE_FORCE_STOP/SUSPEND/DISABLE/HIDE` (1–4, the step numbers the UI shows, in the order `freezeTotal` applies them) plus `isGateApplied`/`canOperateGate`/`setGate`, and the **Freeze sub-box** on the 盗み見 page (`view_freeze_box.xml` + `view_freeze_gate_row.xml`, bound by `AppDetailsSnoopingFragment.bindFreezeBox`). **Why it exists**: Android Auto refuses to run against a `FREEZE_TOTAL`-frozen Google Maps and runs happily against the same Maps carrying only the disable gate — measured 2026-09-12 on the second phone, where `dumpsys package com.google.android.apps.maps` reads `installed=true hidden=false suspended=false stopped=false enabled=3`, `lastDisabledCaller: 2000`. The discriminator is **installedness**: a disabled package is still listed by a plain `pm list packages` (verified) while its activities stop resolving (`cmd package resolve-activity` → `No activity found`), whereas a hidden one is reported as *not installed* and a presence check on it throws. Nothing in the app could express that state, because every freeze was a **method** — one value, applied wholesale — so the box gives each gate its own switch and 白い熊 can find the rung an outside app trips over by driving. **These are NOT a new freezing method and nothing persists them**: `FreezeUtils.isFrozen` is still the app-wide truth (suspend/disable/hide — *not* stopped, so a phone carrying only step 1 is a phone with nothing frozen and the master switch reads off), the `FREEZE_*` values are still the wire format, and the box is a second door onto the same platform calls. **The master switch is `FREEZE_TOTAL` on / `unfreeze` off and deliberately raises no method picker** — a switch that opens a dialog is not a switch — and it leaves the per-app remembered method alone, or it would silently change what the main list's snowflake does next. **`setGate` measures rather than infers**: it re-reads `ApplicationInfo` (with `MATCH_UNINSTALLED_PACKAGES \| MATCH_DISABLED_COMPONENTS`, or the hide gate resolves to nothing) and answers whether the state actually moved — a suspension lifted in the wrong slot returns looking like success, `hideBestEffort` reverts itself when the row would vanish, and a PERSISTENT process is back before the force-stop returns. **Step 1 needed a release that did not exist**: a force-stop leaves `stopped=true` and the only ordinary way out is launching the app, so `PackageManagerCompat.setPackageStoppedState` (guarded by `CHANGE_COMPONENT_ENABLED_STATE`, which this shell holds) is what makes it a two-way switch; `FreezeUtils.unfreeze` still does not clear it, and the master switch clears it separately. **Applying a gate goes through the 必要 guard; releasing never does** — an app added to the profile after it was frozen must still be thawable, which also fixed the old unfreeze path on this page. A gate this phone cannot operate keeps its number, name and explanation and loses only its switch (drawn at 0.55 alpha, its reason in place of the chip): the ladder is also the page's account of what a freeze *is*, and a rung silently missing explains nothing. **LANDMINE — `<include>` layout params**: `LayoutInflater.parseInclude` uses the `<include>`'s own `layout_*` whenever it can build them and falls back to the included root's only when that **throws**, which is what a missing `layout_width` does — so giving the freeze-box include a width/height would silently drop the card's `layout_marginTop`. The four step rows share every id inside them, so each row is resolved once in the view holder and every later lookup is scoped to the row. Replaced the card's "Suspend this app" row and its "Freeze" pill; uninstall stays as a half-width pill with a weighted spacer beside it. |
| **LANDMINE — a hidden app is invisible to a query that does not ask for it** (白い熊, +29) | `PackageManagerCompat.isPackageHidden` looked the package up **without** `MATCH_UNINSTALLED_PACKAGES`. A hidden package is *precisely* one the platform reports as not installed for the user (`PackageUserState#isAvailable` = `installed && (!hidden \|\| matchUninstalled)`), so that query threw `NameNotFoundException` for exactly the packages it was asked about, fell through to the `MANAGE_USERS` reader this phone's shell cannot use, and answered a flat **"not hidden"** — whereupon `FreezeUtils.unfreeze` skipped the reveal and **a hidden app could never be thawed again from the app**. Latent and harmless until +29, because nothing here had ever succeeded in hiding anything. The same trap governs every surface that can meet a frozen app: `AppDb`'s enumeration, `ApplicationItem`, `AppDataTransfer.isFrozen` and the battery screen already pass `MATCH_UNINSTALLED_PACKAGES \| MATCH_DISABLED_COMPONENTS`; **any new lookup must too**. Paired guard, because the row is the only way back: `hideBestEffort` re-reads the package after hiding through the **exact predicate the list uses** (`ApplicationInfoCompat.isInstalled`, which is what `App.fromPackageInfo` sets `App.isInstalled` from) and **reverts the hide** if the app would not get a row. Hiding sets `PRIVATE_FLAG_HIDDEN` and leaves `FLAG_INSTALLED` alone, so by construction the row survives — but this phone had never once executed a hide, so the assumption was untested and is now checked rather than trusted. `revealBestEffort` is the single place that knows both doors (shell, then delegate) and is used by unfreeze and by the revert. |
| Uninstall chokepoint | `app/src/main/java/io/github/muntashirakon/AppManager/apk/installer/PackageInstallerCompat.java` |
| Customisable selection toolbar registry | `app/src/main/java/io/github/muntashirakon/AppManager/main/MainToolbarPrefs.java` — add new actions to `ALL_KEYS` + `idForKey`/`titleForKey`/`iconForKey`. An item in the menu XML alone is **invisible**, because the toolbar is rebuilt programmatically. |
| **Retiring a registry key moves the pill unless you say otherwise** (白い熊, +038) | `MainToolbarPrefs.alias(key)`. Both pill registries drop a stored key they do not recognise and then append anything missing from both lists to the **end** of the visible order — so renaming a key is not a rename: the pill silently jumps to the back of the pane. `alias` maps the retired name to its replacement while the stored order is parsed, which keeps the position and means the old name never has to be recognised anywhere else. First user: `clear_data_cache` → `clear_data`, the batch bar's "Clear" pill (it opened a dialog asking data or cache; it is Clear data outright now, and batch **clear cache** therefore lives only in One-click ops and the pane's own hidden `clear_cache` pill). **Sister landmine in `AppPanePrefs`**: adding a key to `DEFAULT_VISIBLE` is **inert** on any install whose pane has been edited — once both stored orders exist, reconciliation only promotes a key missing from *both*, and a key that was already in `ALL_KEYS` is sitting in the hidden list for ever. Hence `AppPanePrefs.migrateClearDataVisible`, a one-shot promotion called from `AppManager.onCreate` beside the freezing-method migrations, same shape and same reason. The pane's `clear_data`/`clear_cache` keys had existed since +118 with **no implementation** — they fell through to the default branch and opened App info — and now run the ordinary batch op over one package via `MainActivity.runSingleAppOp`, so a single app is a batch of one and gets the progress page and the operation log rather than a second implementation. |
| Toast theming (black/yellow) | `app/src/main/java/io/github/muntashirakon/AppManager/utils/UIUtils.java` routes every `displayShortToast` / `displayLongToast` / `displayLongToastPl` through a custom view (`layout/toast_shiroikuma.xml`). Colours/border now come from the **configurable fork theme** at runtime via `ForkThemeUtils` (the static `drawable/bg_toast_shiroikuma.xml` is no longer the source of truth). Custom toast views only render in the foreground on Android 12+. |
| Configurable fork theme | `app/src/main/java/io/github/muntashirakon/AppManager/utils/ForkThemeUtils.java` is the single source of truth for text/background/border colour + border width (prefs `PREF_THEME_*` in `AppPref`, accessors in `Prefs.Appearance`, UI in `preferences_appearance.xml` + `AppearancePreferences`). Styles **both** the themed toasts and the batch-progress dialog. Defaults reproduce the original hard-coded bright-yellow-on-black look. |
| Batch-op progress **page** + pause/cancel | Full screen, not a dialog (白い熊, +116 — `main/BatchProgressDialog.java` and its two layouts are **deleted**). Two singletons behind it, and the split is the point: `batchops/BatchOpsProgressMonitor.java` answers *what is happening now* (a handful of in-flight items, replaced as they move on — right for a bar, and it throws away exactly the history a log is read for), while `batchops/OpLog.java` keeps *what has happened* — every app, stage and file, capped at 20 000 lines and trimmed in blocks of 2 000. The service calls `begin`/`finish` on both, the worker loop calls `publishProgress` and the per-item checkpoint, and the page observes both. The single chokepoint is still `BatchOpsManager.updateProgress(...)` (first call in every op loop) — it blocks while paused and throws `OperationCancelledException` on cancel, caught in `performOp(info, handler)`. **LANDMINE — backup and restore run one thread per core, so events from up to eight apps arrive shuffled.** A plain indented tree would then file one app's files under whichever app printed last, so `OpLog.ensureContext` re-announces the owning app (`Entry.continued`, drawn faded with `↳`) whenever the emitter changes: every line belongs to the app named directly above it, and nothing is inferred from position. Publication is a revision counter, not a copied list — `postValue` keeps only the newest pending value, so a burst from eight threads collapses into one redraw. **The log outlives the page**: folding the Mate XT destroys and rebuilds the activity, which is a normal thing to do during a long backup. Depth ladder = batch 0 · app 1 · stage 2 · item 3, rendered by `OpLogFormat` — the one renderer, shared with `OpLog.asText()` for Copy/Save, or the saved log would stop matching the read one. **A failure's reason gets a line of its own** (白い熊, +157): appended to the summary it became the tail of `白い熊 自由作業盤 · 2.1 s · 14.1 MB · Row too big to fit into CursorWindow…`, and since rows are middle-ellipsized what survived on screen was `14.1…to CursorWindow` — the size and the end of a sentence, with the part that names the failure elided. `OpLog.appFinished` now emits the summary and then the reason one level deeper, so it starts at column 0; both surfaces follow, because the renderer is shared with `asText()`. Rows are monospaced, single-line, ellipsized in the **middle** (a wrapped line restarts at column 0 and reads as a new entry; a proportional face breaks the ladder), and a tap shows the whole line. Depth comes from `BackupProgressListener.onItem` (a `default` no-op added beside `onStage`/`onDestination`/`onBytesWritten`), fed by `BackupOp.reportBytes`/`RestoreOp.reportFiles` naming every archive member with its real on-disk size, plus counts for extras and rules and the sister app's own app-data progress. `BatchOpsManager.itemListener` feeds monitor **and** log, remembering the last stage so a leaf does not blank the header. Opened from two places, because one is not enough: `MainActivity`'s `ACTION_BATCH_OPS_STARTED` receiver only exists while the app list is resumed, so `BatchOpsService` also launches it (needs `FLAG_ACTIVITY_NEW_TASK`; a refusal under background-launch rules is a silent no-op and the notification still opens it). **Rule (白い熊): always for backup/restore/backup-APK/delete-backup/import-backups, otherwise only when more than one app is involved** — `BatchOpsProgressActivity.shouldAutoOpen`. Gated by the `batch_progress_dialog` pref, whose key is unchanged so old exports still carry it. Colours are `ColorPrefs.OPLOG_*` and the size/timestamp toggle `fonts/OpLogPrefs.java` (own prefs file, classified APPEARANCE), settable in the *Batch operation log* group on the 白い熊 応用管理 UI page. |
| Batch-op completion snap (main list) | `MainViewModel.applyBatchOpResult(op, result, packages, failed)`, called from `MainActivity`'s `ACTION_BATCH_OPS_COMPLETED` receiver, snaps the list to its final state in **one** pass instead of letting the system's throttled per-package change broadcasts repaint it row-by-row over many seconds. For ops with a cheap in-memory state change it mutates the affected rows directly, then re-sorts + re-filters once: **freeze/unfreeze** → `ApplicationItem.setFrozenStateForBatchOp(frozen)` (keyed off `freezeTargetForOp`), via the shared `snapApplicationItems(targets, mutator)` helper; **reinstall** (`OP_INSTALL_EXISTING`) → `ApplicationItem.setInstalledStateForBatchOp()` (flips `isInstalled=true`, so rows drop out of the *Uninstalled apps* filter at once); **uninstall** (`OP_UNINSTALL`, `keepData=false`) → `snapUninstalledApplicationItems(...)`, which per-row mirrors `AppDb.updateApplicationInternal`: an **updated** system app stays installed (left untouched), a **pure** system app or any app **with a backup** flips to `isInstalled=false`, and a user app **with no backup** is removed outright. Any **other** op falls back to a targeted re-read (`updateInfoForPackages`). The optimistic in-memory branches only run when the op `ranToCompletion` (`result != RESULT_CANCELED`); on cancel the package list names every queued app but the unprocessed ones weren't changed, so it falls back to the true-state re-read. The trailing OS broadcasts then merely confirm the same state, so there's no visible row-by-row churn. When adding a new op whose result changes a filterable flag, give it an in-memory snap branch here rather than relying on the re-read/broadcast path. |
| Profile pills (per row) | `MainRecyclerAdapter.mPackageToProfileNames`, rebuilt by `loadProfileMembership()`. Trigger via `reloadProfileMembership()` on pull-to-refresh, on resume, and on the `AddToProfileDialogFragment.RESULT_KEY` fragment-result signal sent after a successful add. **Right-aligned since +94** (see the row-pill entry below): the strip lives in a `HorizontalScrollView` with `fillViewport="true"` over a `LinearLayoutCompat` at `gravity="end"` — that pair is the whole trick, stretching the strip to the viewport so `end` has something to push against, and scrolling sideways rather than wrapping when there are more pills than fit. The `+` is **outside** the scroller, so it can never be scrolled out of reach. They are no longer `Chip`s in a `ChipGroup`. |
| Per-app notes (main list + app details) | Free-text note attached to a package. Store: `utils/AppNotesManager.java` — a dedicated SharedPreferences file `shiroikuma_notes.xml` (key = package name, value = note; blank text **deletes** the key). Keyed by package only (multi-user packages share one note). The view/edit dialog is the shared static `AppNotesManager.showNoteDialog(ctx, pkg, label, onSaved)` (wraps `TextInputDialogBuilder`; multi-line, pre-filled, Save persists, blank deletes; optional `onSaved` runnable) — called by both `MainRecyclerAdapter.showNoteDialog` (onSaved = row refresh) and the app-details **Note** horizontal action button (`AppInfoFragment.getHorizontalActions`, an `ActionItem` before Uninstall; onSaved = null, the main list re-binds on resume). Because the store is its **own** `shared_prefs/*.xml`, **Settings export/import covers it automatically** with no change to `SettingsBackupManager`. On Save it refreshes just that row via `notifyItemChanged(holder.getBindingAdapterPosition())` (re-read at save time, **never** the stale bind position). Strings `note` / `note_blank_deletes_helper`. **UI: one pill in both states (白い熊, +94)** — `@id/note_pill`, right-aligned at the end of the label line, bound by `MainRecyclerAdapter.bindNotePill`. With a note it shows the note glyph plus the note's **first line**, ellipsized to what the row can spare (the full note is the `contentDescription`/tooltip); with none it is the same pill holding only `drawable/ic_note_add_24dp.xml`, pinned to `RowPills.ADD_WIDTH_DP` so it is exactly as wide as the tag `+` beneath it. It replaced a compound `drawableEnd` on the `label` (has-note) plus a `+` floating over the column's top-right (no-note): two controls for one affordance, in two places, neither able to show what the note *said*. **How wide it grows is settled at measure time, never at bind time** (白い熊, +95 — +94 guessed a fixed half of the line and left a gap between a short app name and the note): `main/LabelLineLayout.java` is the label line, and it exists for one rule — **the app name in full and not one pixel more; the note takes all the rest, reaching back to exactly where the name ends**. The name is `wrap_content` and **unweighted**, `@id/note_slot` (the `FrameLayout` around the pill, `layout_gravity="end"`) is the **only** weighted child, so it absorbs every remaining pixel. Weights alone cannot finish the job: weight on both splits the surplus (the +94 gap), weight on the slot alone leaves it **0 px** — the note gone from the row — whenever the name is longer than the whole line. ConstraintLayout answers this with `layout_constrainedWidth` against the pill's minimum and **`ConstraintLayout` is not a dependency here**, so `LabelLineLayout.onMeasure` caps the label at `available − (the other children + their margins + RowPills.ADD_WIDTH_DP)` before calling super — below the cap the name is untouched, above it the name ellipsizes (as it must anyway) and the note keeps its floor. Floor of the cap is **half the line**, or a 3–4-column grid would reserve away the whole name. `setMaxWidth` is called only when the value changes (it requests a layout). The **debug star sits before the pill**, since it is INVISIBLE rather than GONE for a non-debuggable app and would otherwise hold the pill ~24dp off the column's end, breaking the shared edge. The pill repeats the app icon's two guards (+82): a tap in **selection mode** extends the selection, and `setOnLongClickListener(v -> itemView.performLongClick())` hands the long-press back to the card (a clickable child swallows it, and this child is half a row wide). |
| **Row pills are built in one place** (白い熊, +94) | `main/RowPills.java` — the note pill, the tag pills and the two **add** affordances all come from this one builder, because the ask was that the add controls be *uniform*: same stadium outline, same pinned width, same faded ink, stacked into one column at the row's right edge. Two styling blocks agreeing with each other today is not uniformity; one builder is. Shape = the fork's pill language, shared with the Snooping status pills (transparent fill, hairline stadium — corner radius past half the height, which the framework clamps — plus a ripple of the same hue), built in code because the accent is the configurable theme's. **Alpha carries the hierarchy**: an add affordance is faded (0.55) — it is an invitation, not information; a written note is drawn below full strength too (0.45 outline / 0.80 ink) so it reads as a margin note rather than a second title; a tag pill alone is full-strength, being a fact about the app. `tagPill` returns **`LinearLayoutCompat.LayoutParams`**, not the `ViewGroup.MarginLayoutParams` it descends from — LinearLayout converts foreign params through `LayoutParams(ViewGroup.LayoutParams)`, which copies width and height and **silently drops the margins**. `battery/MainCardBinder` binds the same pills through the same builder (its note pill now works — it used to show a `+` with no listener behind it), so the battery header and the list cannot drift. |
| Settings export/import | Category-based (Kōjiki flow), on the **Export/Import section at the top of the 白い熊 応用管理 UI page** — the old "Settings export / import" category on the Backup/restore page is gone. `settings/SettingsBackupManager.java` bundles `shared_prefs/*.xml` + `files/profiles/` + `files/fonts/` (zip layout unchanged, so pre-category archives still import); its `Category` enum (GENERAL/APPEARANCE/MONITOR/TOOLBAR/NOTES/PROFILES) classifies every entry by prefs-file name on both export and import (`classify` — unlisted files fall into GENERAL, so nothing is ever dropped), and each carries a **stable wire `id`** (`general`/`appearance`/…) that the automation contract's `items` extra accepts — **never rename those**. The **archive name follows the family convention** (白い熊, 2026-07-25): `shiroikuma-oyokanri_<yyyy-MM-dd_HH-mm-ss>.zip` — no version, no infix, no suffix, because every sister app's backups share one directory; the pre-convention `AppManager-settings_` prefix survives only as `LEGACY_EXPORT_PREFIX`, still matched by the "last export" query. `writeExport(context, categories, OutputStream, ProgressListener)` is the **headless core**; the panel and `StateExportReceiver` are its two thin callers (never duplicate export logic in a caller). `settings/SettingsExportImportPanel.java` is the panel: bordered black/yellow box (transparent dialog window), tappable export-directory box (plain-path browser `settings/DirectoryChooserDialog.java`, shared with the backup-dir pref), a "Last export:" line (newest export zip by mtime; warn-red when unset/empty — also shown in the UI-page row summary, re-queried on page open + panel close), Select-all + category checkboxes, and an ArcaneChat pill row (Cancel left, Import/Export right). Success info dialogs are bordered; acknowledging them (OK on export, "Later" on import) **closes the whole chain** — info dialog → panel → UI page (back-dispatcher); "Restart now" hard-kills via `Process.killProcess(myPid)` — **not** `Runtime.exit` — or cached `SharedPreferences` will clobber the imported files on orderly shutdown. Failures (toast) leave the panel open. | **An import may TIGHTEN a security setting and never loosen one (白い熊, +117).** `settings/SecurityPrefGuard.java`: import replays whole prefs files, so an archive — months old, from another phone, or handed over by someone else — could switch off the app lock, turn off install verification or disable backup encryption, and a wholesale file copy could never notice. Guarded: `enable_screen_lock`, `enable_auto_lock`, `installer_disable_verification`, `installer_block_trackers`, `log_viewer_omit_sensitive_info` (booleans, each with the value that IS the safe one) and `encryption` (safe = **any** mode but `none`, so switching AES→PGP is a choice and AES→none is not). **The asymmetry is the design**: a setting is held only when the CURRENT value is the safe one and the archive would move it away — **including by omitting it**, since a missing entry reverts to the default and the default is not always safe (`encryption` defaults to `none`). A tightening always passes. **LANDMINE — it must edit the FILE, never the SharedPreferences API**: import replaces prefs files while the process still holds them cached (which is why "Restart now" hard-kills), so any `edit().apply()` here would write the cached map back over the file just imported and undo it; the correction is targeted regex surgery on the XML entry on its way to disk, inserting the entry when it is absent, and any unexpected shape passes the bytes through unchanged — a guard that breaks an import is worse than the loosening it prevents. What was held is **named in the success dialog** (`settings_eim_import_held`), because a silent policy is indistinguishable from an import that did not work.
| 保存復元 automation contract (headless export) | The wire contract 白い熊's automation app **白い熊 自由作業盤** (`shiroikuma.jiyusagyoban`) fires at every sister app to back them all up in one run. `settings/StateExportReceiver.java` (manifest-registered, `android:exported="true"`, **no `android:permission`** — the caller cannot hold one, the token is the gate) answers two actions: `shiroikuma.oyokanri.action.LIST_CATEGORIES` (instant; `OK:` + one `id<TAB>label` line per category — flat here, so the optional third parent-id field is always omitted) and `.action.EXPORT_STATE` (runs `SettingsBackupManager.writeExport` headlessly into **exactly ONE** zip). Directory precedence: the `path` extra → `Prefs.Storage.getSettingsExportDirectory()` → `ERROR:no-directory`; both are plain absolute paths and the app holds `MANAGE_EXTERNAL_STORAGE`, so it writes with plain `java.io.File`. `items` = comma-separated `Category.id`s (absent/empty = everything; any unknown id → `ERROR:unknown category in items: <items>`, nothing written). Reply on success: `OK:<abs path>\|<bytes>\|<human size>\|<n> categories`. **Hard-won EMUI constraints — do not "improve" them** (verified 2026-07-23): the reply is a **fresh `sendBroadcast`** with `setPackage(reply_package)` + **`FLAG_INCLUDE_STOPPED_PACKAGES`** (without it a backgrounded caller never hears us); **never** a `ResultReceiver`/`PendingIntent`/`Messenger` (EMUI may drop a broadcast carrying a live Binder outright), and the ordered-broadcast result is set but is **never the only reply** (EMUI severs that channel between third-party apps). Exactly one terminal reply per request, guarded by an `AtomicBoolean`; the work runs on a background thread under `goAsync()`. **LANDMINE — the progress label travels in `text`, NOT in `result` (白い熊, +181, measured across all 57 sister repos).** `result` carries the **terminal reply** and nothing else. 自由作業盤's `StateExportReceiver.kt` — the reference implementation every sister app mirrors — defines `EXTRA_PROGRESS_TEXT = "text"`, this app's own `StateExportReceiver` sends `"text"` too (line 308) with `"result"` only on the terminal reply (line 334), and a sweep found **26 of the 31** sister apps that report progress sending `"text"` alone. `AppDataClient` nevertheless read the label from `EXTRA_RESULT` for months, so it emitted one key and read another and **silently discarded the progress label of essentially the whole family** — which is why long transfers looked mute, and why two sister chats were sent to "fix" apps that were correct. `AppDataClient#progressLabel` now reads `text` and falls back to `result` (three apps send both). Do not "tidy" this back to a single key, and do not conclude from a silent transfer that the app is sending nothing. Progress (when `progress_action` is present) is **real counts, never a percentage** — `区分 i/N — <label>` with `current`/`total` (long) + `unit` extras, throttled to one per 500 ms plus a mandatory final one. `Log.d(TAG, …)` on receive + reply is the **only** on-device trace (the reply goes to another app). Gate: `settings/AutomationAuth.java` — **v2 shape (白い熊, +151), mirroring 自由作業盤's own `AutomationAuth` so every sister app behaves the same**: `shiroikuma_automation.xml` (own prefs file) holds `automation_enabled` **default true** and `automation_require_token` **default false**, plus the 24-byte `SecureRandom` hex token (generated lazily, compared **constant-time** via `MessageDigest.isEqual`). v1 shipped every app closed and a caller had to present a secret pasted out of this app's settings — which cannot survive a wipe, and the case this family now serves is 応用管理 restoring a **clean** phone, where nothing has been pasted. So the switch ships on (it stays, as the only way to close this app off) and the token is an extra a caller may be *asked* for. **A token sent to an app that does not require one is IGNORED, never refused** — tokens outlive the setting they were pasted for, and another app in the same batch may still want one; refusing would turn one switch into half a batch failing. **The whole check is `AutomationAuth.refuse(context, token)`**, returning `null` or the exact `ERROR:` line — one place, because two checks written out per entry point is how "disabled" and "bad token" drift apart across forty-odd apps. UI order: master switch · *Use an authorization token?* · the token row, which is **GONE while the requirement is off**. The prefs file stays in `EXCLUDED_PREFS`, and with these defaults that costs nothing on a migration: a fresh install comes up answering the batch with no pairing at all.</br>Historic v1 note: `SettingsBackupManager.EXCLUDED_PREFS` keeps that file out of **both** export and import, so the token never travels in a backup nor gets planted by one. UI = two rows appended **inside the Export/Import section** of the 白い熊 応用管理 UI page (never a section of its own — every sister app looks the same): the master switch, then the token row (abbreviated `8…8`, tap copies the full value, Regenerate warns pasted copies go stale) — `FontsPreferences.buildAutomationSwitchRow`/`buildAutomationTokenRow`. |
| Profile file resolution | `ProfileManager.resolveExistingProfilePath(profileId)` returns the actual file by matching its stored id (scanning if the canonical name doesn't exist). Use this when writing back to a profile rather than `findProfilePathById`, which assumes the filename stem == id. |
| App-icon cache (main list) | `app/src/main/java/io/github/muntashirakon/AppManager/self/imagecache/ImageLoader.java` caches icons in two layers — in-memory `LruCache` + on-disk `images/<tag>.png` (`ImageFileCache`, 7-day-mtime GC, never a full wipe) — keyed by a **version-unaware tag**, so a reinstall keeps the **old** icon until the GC. The main list keys each row by `ImageLoader.versionedTag(packageName, lastUpdateTime)` → `"pkg@time"` so a reinstall busts both layers. **Set the same versioned tag on both `holder.icon.setTag(...)` and `displayImage(...)`** (`MainRecyclerAdapter` ~line 464) — they must match or the recycled-view guard never binds the bitmap. Feed it a **live** `lastUpdateTime`: `MainViewModel.getNewApplicationItem` sources it from `getPackageInfo` (not the DB-cached `app.lastUpdateTime`, which lags reinstalls — the "some icons refresh, some don't" cause). Other icon surfaces (app-details, usage, running-apps, etc.) still use the bare `packageName` key. |
| **Pill shelf under the toolbar** (白い熊, +118) | `main/ShelfView.java` + `main/ShelfPrefs.java` (own prefs file `shiroikuma_shelf`, JSON, classified **TOOLBAR**). A pill is either a **saved view** — filter flags + sort + reverse + profile filters + search query, captured together because they only mean anything together — or a **screen**. `+` sits **outside** the scroller (the +94 trick: a `+` inside a scrolling strip is the one control you cannot reach once it overflows). The strip is a horizontal `RecyclerView` **because reordering has to be a drag**, and `ItemTouchHelper` does drags properly; the order is persisted on **drop**, not per move. Tapping the active pill **clears back to the plain list**, so one pill is both the way in and the way out. Applying goes through `MainViewModel.applyView(...)` — one pass that sets everything and runs the pipeline **once**; the individual setters would re-filter five times and re-sort twice per tap. Two of the ready-made presets (recently changed, biggest apps) exist here precisely because they do **not** deserve a screen: they are this list with a sort. |
| **Lenses — a "special page" is a state of the main list** (白い熊, +162…+167) | `main/lens/MainLens.java` + `MainLenses` (registry) + `BackupsLens` · `SnoopingLens` · `SisterAppsLens`. A lens changes **what some row elements show** and which apps are on the page; everything else — filters, search, profile filters, saved views, selection, the batch pane, the app pane, the frames, per-geometry columns, the separators — is the main list's own and untouched. The plain list is "no lens", and the way out is the way in: tapping the lit shelf pill again. **The ids are the SAME strings the shelf already stored** (`ShelfPrefs.SCREEN_BACKUPS`/`SNOOPING`/`SISTER`), so pills made before lenses existed keep working with no migration — never rename one. **What a lens deliberately cannot do**: take over the row's click (a tap unrolls the pane, +118, and keeping that was the point) or contribute filter flags (those are a persisted bitmask *and* the wire format of a saved view; a lens narrows through `includes()` instead). Rendering goes through **`MainCardBinder.renderCustomRightLines`**, extracted so the lens and the remaining tracker screen cannot drift. **LANDMINE — `prepare()` runs BEFORE the include filter**, over every candidate: for two of the three lenses membership *is* what preparation works out (盗み見 needs a `SnoopingResolver` pass, 仲間 needs `GET_META_DATA`), neither of which an `ApplicationItem` carries. **Caching is not optional**: a keystroke re-runs the whole pipeline, so an uncached 盗み見 would re-resolve every app per character — verdicts are cached per package against `lastUpdateTime` (an update is what changes what an app can do, and moving that timestamp is how an update announces itself), and both lenses check `ThreadUtils.isInterrupted()` so a keystroke mid-pass cancels and resumes from the cache. Pull-to-refresh calls `MainViewModel.invalidateLensCaches()`, which is the only thing that clears them. The single chokepoint is `MainViewModel.publish(...)` — every `postValue` in the filter path goes through it. `ShelfView.setActiveId` is view state while the lens is view-model state, so `MainActivity.relightLensPill()` reconciles them after every shelf reload (folding the Mate XT rebuilds the activity). **Battery and Process stay real screens** — their rows are per-uid and per-pid, not `ApplicationItem`s. |
|---|---|
| **Sort is its own control, not part of the filter sheet** (白い熊, +163…+165) | `main/SortMenuPopup.java`, opened by `action_sort` on the top bar right of clear-filters. A filter is set once and left; a sort is flicked between while looking at the same list, so it costs a tap rather than an open-scroll-dismiss. `MainListOptions.getSortIdLocaleMap()` returns **null** (which `ListOptions` already treats as "hide the sort section"), and the orders themselves live in the new static `MainListOptions.sortIdLocaleMap()`; every other `ListOptions` subclass keeps its in-sheet sort. The active lens's own orders are appended to the same panel under a heading, but keep a **separate id space and a separate view-model field** — the list's sort ids are the wire format a saved view stores, while a lens index means something different in every lens. **A framework `PopupMenu` cannot be used**: its row height, padding and lack of separators are baked into the platform's layout, so the panel is drawn by hand — rows packed to the text, a hairline between them, a wash plus a filled marker on the selection, and the kxkb text-width underline for the group heading. It hugs its content and otherwise runs to the bottom edge, measured from the anchor rather than assumed (the Mate XT's window height differs folded, unfolded and in multi-window). Paired fix: `MainViewModel.setReverseSort` used to submit its re-sort *before* assigning the new value and read the field rather than a local — a race that only showed once Reverse became a one-tap toggle. |
|---|---|
| **Sibling screens** (白い熊, +118; reduced to one, +167) | `screens/ListScreenActivity` now serves **only** the tracker screen (`TrackerAppsSource` — every other app carrying one tracker, entered from a tracker pill, so it takes an extra rather than an id). `BackupsSource`, `SnoopingSource` and `SisterAppsSource` are **deleted**: they are lenses now. `sourceFor` therefore requires the tracker extra and the activity finishes without one — the old default built 保存一覧, which after the change would have silently opened a page with no way in. The selection bar's back-up/restore/delete pills went too (under the Backups lens those are the main list's own batch pane, which is strictly more), and *Clean up backups* moved to the main overflow, shown while the Backups lens is on, in addition to Settings → Backup/restore. Historical note — the three deleted sources were: **保存一覧** (`BackupsSource` — every package with backups **including uninstalled ones**, which is the value: backup vs installed version with the +105 staleness rule, count, size on disk walked from the backup directory, date, and what is actually inside), **盗み見一覧** (`SnoopingSource` — apps by capabilities allowed right now, through `SnoopingResolver`, the same resolver the tab and the enforcer use, so the three cannot disagree; **the expensive one** — a resolver pass per app, background-loaded, refreshed only on pull-to-refresh), **仲間** (`SisterAppsSource` — contract/format read from the manifest via `AppDataContract.fromMetaData`, so **nothing is woken** and frozen apps still appear; whether the last backup carried app data, and the category choice). One row model (`ScreenRow`) and one adapter for all three: they share the main list's **colour keys and row shape, not its code** — the +30 rule. Rows are clickable into what the screen implies (backup dialog · 盗み見 tab · category picker). New screens must be added to `AppearanceUtils.isYellowOnBlackActivity`. |
| **A tap unrolls a pane; it does not leave** (白い熊, +118) | `main/AppPaneBinder.java` fills `@id/app_pane`, a `GONE` container at the bottom of `item_main.xml`, which is now a **column** (row, then pane). **LANDMINE — the pane is part of the row and NOT an inserted adapter item**: every selection call in `MultiSelectionView.Adapter` is keyed by **adapter position** (`select`/`deselect`/`isSelected`/`selectRange`), so an inserted item would shift every position after it and quietly corrupt the selection. Width in a grid is handled by a **span-size lookup** instead (`MainActivity.applyPaneSpan`), which changes how one position is laid out and touches no data; the span count is read **inside** the lookup because `AutoFitGridLayoutManager` recomputes it on measure. One pane at a time; `notifyDataSetChanged` on toggle because **both** rows' span sizes changed; Back closes it (`collapsePane`). **The whole row agrees**: card, label, icon and backup column all unroll it — the icon's +82 shortcut to 盗み見 is gone, and 盗み見 and 応用情報 are pills inside instead. Pills come from `main/AppPanePrefs.java`, the same visible/hidden+order shape as `MainToolbarPrefs`, edited by the **same fragment** (`MainToolbarPreferences` now takes a `registry` extra) — unlike the toolbar, not everything starts visible. Actions route to the existing single-app flows; the pane adds a way to reach them, never a second implementation. Pills wrap via `main/FlowLayout.java`, which is why their labels are readable at all. |
| **Batch actions are a pane, not a bar** (白い熊, +118) | `@id/batch_pane` in `activity_main.xml`, built by `MainActivity.setupBatchPane`. The legacy `MultiSelectionView` bar rendered a **menu** into a strip, which is why its labels were cut off. Same actions, same configured order (`MainToolbarPrefs`, so the existing settings page keeps working), as wrapping pills; **every pill dispatches through `onNavigationItemSelected(menuItem)`**, the menu-id dispatcher that already existed, so no batch logic is duplicated. Each pill's enabled state mirrors its `MenuItem`, so `MainBatchOpsHandler`'s constraints still hold. Foldable to its header. `MultiSelectionView` keeps doing the bookkeeping with its face hidden: **`setBarSuppressed(boolean, int)`** (libcore) skips the transition and the visibility change but still sets selection mode, fires the listeners and feeds the bottom padding — the *external* height, reported back from the pane's layout listener, so the last row still clears it. Other screens using the widget are untouched. |
| **Backup parts have colours, and every surface uses them** (白い熊, +121…+133) | `backup/dialog/BackupParts.java` is the legend — one colour and one glyph per part (APK ice blue, internal data theme yellow, external amber, app-supplied green, OBB/media violet, cache grey, extras rose, rules orange) — and `BackupPartRows.java` draws it everywhere a part is chosen: the single-app options list (`partRow`), the batch table (`partPill`), the category pills (`plainPill`), the restore chooser (`showRestoreOptions`). The palette is **fixed, not settable**: it is a legend, and a per-screen colour picker would make it worth nothing. `BatchBackupTableDialog` is the batch backup surface — one row per app with its own parts and a foldable app-supplied section — because one set of flags for five hundred apps is only right when they are all the same kind of thing. `RestorePartsTable` is its mirror, and it exists because batch restore used to build its choice from `getWorstBackupFlag()`, the **intersection** of every selected backup: a part only some archives carried could not be chosen at all, and the apps that had it silently did not get it back. Each app also picks **which** backup to restore from, since the parts are a fact about that archive. |
| **Per-app options travel with the operation** (白い熊, +121/+131/+133/+136) | `BatchBackupOptions` carries three per-package maps beside the batch-wide values — `mPerPackageFlags`, `mPerPackageRelativeDirs`, `mPerPackageAppData` — each in the parcel (parallel arrays, never a Serializable Map) and the JSON. `getBackupOpOptions`/`getRestoreOpOptions`/`getDeleteOpOptions` prefer the per-package entry and fall back to the batch value. **LANDMINE — there were TWO places building these options.** `AppBackupDialogFragment` had its own copy of `startOperation` still calling the three-argument constructor, so every per-app field added since was silently dropped on the path a single app takes: choosing every App-supplied category and pressing OK backed up whatever was stored instead, with identical logs either way. `OperationInfo.toBatchOptions()` is now the one builder and both dialogs call it. |
| **Choosing App-supplied categories: Save vs OK** (白い熊, +133) | `AppDataCategoryPicker` answers two different questions and used to answer only one — every close wrote the app's stored preference, so narrowing one backup narrowed every future one. **Save** stores and applies; **OK** applies to this backup only; **Use app's defaults** forgets the stored choice; Cancel does nothing. A run-scoped choice rides `BatchBackupOptions.mPerPackageAppData` → `BackupOpOptions.appDataCategories` → `AppDataTransfer.export(..., requested, ...)`, where **null means "use what is stored" and an EMPTY array means "none of it"** — the parcel round trip preserves the difference. Requested ids are re-checked against what the app offers right now, because an app updated in between could refuse the whole export with "unknown category". `BatchBackupTableDialog` never writes the preference at all: the table is a working surface you sweep across forty apps, and every tap used to land in `AppDataSelection`. |
| **応用管理 is a sister app too** (白い熊, +134) | `appdata/self/OwnDataProvider.java` at `${applicationId}.automation`, plus the three `shiroikuma.automation.*` manifest metadata (contract 2, format 1 — **integers**, see the `getInt` landmine in `AppDataContract`). It exports exactly what the 保存復元 broadcast already produces: `SettingsBackupManager.writeExport`, with the same `Category` enum as the category list. **The call returns at once and the work runs behind it** — the caller blocks on a broadcast, not the binder, and here the caller is us, so a provider doing the export inside `call()` would deadlock itself; the fd is `dup()`ed before returning. **Who may come through is `appdata/self/AutomationCallers.java`** (白い熊, +153): our own uid by fast path (same uid is the same app), plus an **allowlist of exact package names** — `shiroikuma.oyokanri` and `shiroikuma.jiyusagyoban` — each cross-checked against the uid the kernel reports (`getPackagesForUid`, because packages sharing a uid are not told apart by name) **and** a pinned signing-certificate SHA-256 via `hasSigningCertificate(..., CERT_INPUT_SHA256)`. The pin matters most on the case this whole contract exists for — a clean phone, where an allowlisted package may not be installed yet and its name is a name anyone can take. **Derive a pin with `apksigner verify --print-certs` against the installed APK, never from a hash pasted into a message**; ours is `9c585f4d…c6cc`, 自由作業盤's `efd0d352…c602`. Order in `call()` is **identity → gate → dispatch**, so an unknown caller gets the same refusal for a bogus method as for a real one; another app's call also passes `AutomationAuth.refuse` (our own does not — backing ourselves up is not automation by anybody else), because "turn the switch off to close this app off entirely" has to mean the door too. Until +153 it was self-only, which meant 自由作業盤 could not back 応用管理 up through the door at all. The provider stays *exported* either way, so a stranger gets `ERROR:<reason>` rather than a `SecurityException`, which the client would report as "frozen, or no door". `AppDataTransfer` answers its own category listing directly through `StateExportReceiver.listCategories` rather than broadcasting past its own token gate, and skips preflight/restore for itself — `restore()` would otherwise be entitled to freeze this app when its backup finished. **Importing our own state is STAGED, not applied** (`PendingStateImport`): `importFrom` replaces `shared_prefs/*.xml` while this process holds them cached, and the batch runs *in this process* so it cannot hard-kill the way the Export/Import panel does. Applying it at `Application.attachBaseContext` looks safe and is not — `SecurityPrefGuard` reads current values through `AppPref` to decide what an archive may not weaken, and that read creates the cached instance the plan was avoiding. So it is offered on the next visit to the main list: Restart now / Later / Discard. |
| **The progress page says where you are** (白い熊, +140…+143) | `BackupOp`/`RestoreOp` build a **plan** of the stages the run will actually announce (APK only if the APK is going, KeyStore only if there is one, App-supplied only if the app really has a door) and each announcement looks itself up in it — planned, never counted, because the data step announces itself once per directory and a running counter would walk the total upward while you watch. The number is a `ForegroundColorSpan` in `ColorPrefs.OPLOG_BATCH`, carried on the same CharSequence that feeds the log line **and** the in-flight header. **It is repeated on every progress line**, not only the stage heading: a 6 GB export writes thousands of lines and the heading scrolls hours out of reach. **LANDMINE — `OpLogAdapter.append` used to blanket the appended range with the row colour**, which wins against any span already inside it (for one attribute the span added last draws), so the marker was applied at the source, copied faithfully, and silently repainted; the base colour is now laid into the gaps between existing colour spans. The three passes after the app hands its archive over — storing, encrypting, checksumming — are stages of their own with the size they are about to walk, and restore has the mirror three. A sister app whose byte counter goes **backwards** gets a line saying so: that is a second pass over the same data, and nothing else in the log marks the boundary. |
| **Cancel reaches inside one app** (白い熊, +143) | `BatchOpsManager.updateProgress` checks once per **app**, before that app's work — right for a batch, no use at all for a single app that takes twenty minutes, where Cancel had nothing left to reach and killing the process was the only way out. `BackupOp`/`RestoreOp` now check at every stage boundary (`checkCancelled`), the 6 GB staging copy checks every few megabytes (`copyCancellable` — `IoUtils.copy` is one call over gigabytes and sees nothing raised during it), and `AppDataClient.transfer` takes a `Cancellation` hook consulted by its wait loop, which polls every **1 s** rather than 5 and tells the sister app to stop before walking away. **Both ops rethrow `OperationCancelledException` ahead of their catch-all**, or a cancel is dressed up as "could not back up" and counted as a failure by the batch that asked for it to stop. |
| **Backups are replaced only after the replacement exists** (白い熊, +137) | `BackupItem` writes into a hidden `.name` staging directory beside the target; `commit()` renames it into place and **only then** deletes the previous backups. **LANDMINE — the constructor `mkdirs()`-es the real directory up front and cleanup used to delete only the staging one**, so every failed, cancelled or killed backup left an empty folder standing for ever. `cleanup()` now removes the real directory too, *only when it is empty*. `backup/BackupCleaner.java` + `BackupCleanupDialog` find the ones already on disk — empty folders, half-written `.name` directories, and directories with no readable metadata — reachable from Settings → Backup/restore and the ⋮ menu on 保存一覧. **A directory carrying readable metadata is never a finding**, whatever the database thinks, and an unreadable one counts as a backup rather than as rubbish. The overwrite warning now names what it will do and where "Back up multiple" lives; toggling that switch in the dialog is a decision about **this** backup and is deliberately not written back to the preference (+145). |
| **LANDMINE — `Android/data` is a REAL mount on the Mate XT, not the FUSE view** (白い熊, +044, measured 2026-09-17) | `RestoreOp.restoreDirectory` skips `chown`/`restorecon` for external data on the standing assumption that `/storage/emulated/0/Android/{data,obb,media}` is FUSE-emulated storage, where uid, gid and mode are **synthesised** from the path so nothing written there matters. On this phone that is **false for `Android/data`**: it is its own f2fs partition (`/dev/block/sdd88` on both `/mnt/user/0/emulated/0/Android/data` and `/storage/emulated/0/Android/data`), so extracted entries keep the **archive's own modes** — 0700 dirs / 0600 files, because such a tree was backed up from the app's *internal* data — under `shell:shell`. The app (`u0_aNNN`, group `ext_data_rw`) then cannot traverse its own external files dir: `File.listFiles()` answers **null** and the app crashes on launch, while the restore itself reported success. **`chown` is not the fix and the skip stays** — it is `EPERM` for the shell on that mount — and it is not needed: a file the shell creates there lands `shell:ext_data_rw 0660` and the app reads it happily. Only the preserved restrictive modes break it, so the restore ends with `chmod -R a+rwX` over the tree (capital `X`, so a directory becomes traversable and a data file is never made executable). **Best-effort, never a throw**: the top-level `<pkg>` directory is app-owned and refuses `chmod` with `EPERM` — expected, harmless (it is already traversable), and `chmod -R` walks on past it, so a non-zero exit is the normal case and is only logged. **Internal data is deliberately left alone**: the `chown` there hands the app real ownership, and `a+rwX` on `/data/data/<pkg>` would publish its private files to every app on the phone. |
| **Three apps can never be frozen, suspended or uninstalled** (白い熊, +139) | `ProtectedAppsProfile.ALWAYS_PROTECTED` — 白い熊 雫, 白い熊 応用管理, 白い熊 自由作業盤 — checked **before** the profile lookup, so the answer never depends on the profiles being readable. The 必要 guard fails *open* by design, which is right for a list you edit and wrong for the apps that make this one work: the moment the profiles cannot be read is exactly when a mistake becomes unrecoverable. Every uninstall already funnelled through `PackageInstallerCompat.uninstall` and every freeze through `FreezeUtils.freeze`, but **suspension had escaped both** — the Snooping policy card calls `DevicePolicyBridge.setSuspended` directly, and suspension is the harder freeze; guarded there and at `PackageManagerCompat.suspendPackages`, with **lifting** never blocked. The reaper's `SHELL_PROTECT_SUBSTR` gained 自由作業盤. |
| **The way back into a running or finished batch** (白い熊, +132/+142) | `@id/operation_bar`, pinned in the main `AppBarLayout` like the privilege alarm. Closing the progress page never stopped anything — the work is a foreground service — but the only route back was the notification, so the bar states what is running (or what finished) and opens the page. The progress page's bottom bar is **Pause · Leave running · Cancel**, Cancel alone in red, because with Cancel as the only visible way out that was something you had to risk a backup to find out. `BatchOpsProgressMonitor.noteResultSeen()` is set when the page draws a *finished* operation, and the bar hides on it: closing a log and finding a banner offering the log you just closed reads as a leftover. **LANDMINE — `setHostForeground` was a boolean and is now a counter**: moving from the list to the page runs the new screen's `onResume` before the old one's `onPause`, so the last write said "background" while a window of ours was plainly on top, and the un-themeable system banner flashed over the page announcing it. The completion notification is themed via `RemoteViews` (`notification_shiroikuma.xml`, a box painted the ink colour around one painted the background colour, since RemoteViews can set a colour but not build a bordered drawable) — **only the completion one**: a custom view replaces the template that draws the ongoing notification's progress bar. Since Android 12 the platform's own header strip above it cannot be repainted. |
| Main-list layout picker (top bar) | 3×3 yellow tile-grid icon (`drawable/ic_grid_3x3.xml`, `action_layout_columns` in `activity_main_actions.xml`, last `ifRoom` item so it sits left of the overflow ⋮). Opens a single-choice dialog: **Adaptive (default)** = the original `UIUtils.getGridLayoutAt450Dp` auto-fit grid (one column per 450dp), or fixed **2/3/4 columns** (`GridLayoutManager`). Persisted in `main/MainLayoutPrefs.java` (dedicated SharedPreferences `shiroikuma_main_layout`, key `columns`, 0 = adaptive — covered automatically by settings export/import). `MainActivity.applyListLayout()` swaps the layout manager on `mRecyclerView` at onCreate and on pick. **The pick is per geometry (白い熊, +88)** — see the next row. |
| **One layout per geometry** (白い熊, +88) | A column count that suits the folded cover panel is wrong unfolded and wrong again rotated, so all three list screens (main list, process monitor `MonitorPrefs`, battery history `BatteryLayoutPrefs`) store **one pick per geometry** rather than one pick. `utils/LayoutGeometry.java` is the single place that decides what a geometry *is*: orientation + `Configuration.smallestScreenWidthDp` → keys like `p403` / `l819`, and the pref key is `columns_<that>`. **`smallestScreenWidthDp` is the fold fingerprint** — the smaller side of the window in dp, so it moves when panels unfold (Mate XT ≈ 403 folded, ≈ 819 unfolded) and stands still when the same geometry is merely rotated, which the orientation letter covers. No window-manager dependency and no OEM fold API: a device with three panels or none gets sensible keys for free, and multi-window falls out of the same rule because the configuration has described the *window* rather than the display since API 24. **LANDMINE — pass an activity or view context, never the application context**, or every multi-window geometry collapses onto the full-screen key (`MainRecyclerAdapter` feeds it `cardView.getContext()`; the prefs *file* still opens on the application context, which is unrelated). **Migration:** the pre-geometry bare `columns` key is still read as the seed for any geometry never set, so an existing choice carries into all of them once; writes only ever touch the per-geometry key. Re-applied on geometry change by the activity being recreated (none of the three declares `configChanges`), plus an `applyListLayoutIfGeometryChanged()` guard in each `onResume` — and for the main list inside `refreshForkAppearanceIfChanged()`, so the multi-window side-by-side path (`onTopResumedActivityChanged`) is covered too. |
| Main-list separator grid (no grey between cells) | Cells are edge-to-edge: `item_main.xml` has **no margins**, the RecyclerView background is **black** (matches the cards, so grey can never show — including under shorter cells in a multi-column row), and the adapter sets **corner radius 0 and stroke width 0** per bind — unselected cells draw **no outline at all** (the old per-state strokes — running yellow/orange, uninstalled, disabled — read as random frames once cells touched; the "Card outline" colour rows were removed from the settings screen with them, though `ColorPrefs.STROKE_*` constants remain). The **checked/selected** card's frame is **fully owned by the adapter from prefs** (NOT the M3 checked theming): colour `ColorPrefs.SELECTED_FRAME` (default yellow), border width + corner roundness in `fonts/SelectionFramePrefs.java` (`shiroikuma_selection_frame`; defaults 4dp / 24dp — 24dp = the original `listItemCornerRadius`), with its own "Selected app frame" settings group (`buildSelectionFrameElement`, `view_selection_frame_element.xml`). The frame prefs are read **at bind time** (not cached in `reloadColors`) and the flag-guarded appearance refresh lives in `MainActivity.refreshForkAppearanceIfChanged()`, called from **both** `onResume` and `onTopResumedActivityChanged` — on the tri-fold, settings + main list can be resumed side by side (multi-window), where switching windows never fires onResume, which made setting changes appear to have no effect. The settings page is titled **白い熊 応用管理 UI** (`pref_fonts`) and is also opened directly by **long-pressing the toolbar overflow (hamburger) button** (`MainActivity.attachOverflowLongPress` — the overflow button is the only `ImageView` child of the toolbar's `ActionMenuView`). The page is **kxkb-styled**: every section heading (`view_font_group.xml`, 20sp bold) and sub-heading (17sp bold) carries a TEXT-WIDTH yellow underline (a match_parent View inside a wrap_content vertical LinearLayout — that's the whole trick), sections are separated by a full-bleed 1px yellow hairline (`@id/group_hairline`, hidden on the first section by a pass in `FontsPreferences.onViewCreated`), and the indent ladder is 36dp headings / 54dp sub-headings / 72dp controls & rows. **Never derive the radius from `MaterialCardView.getRadius()`** — M3 corner sizes resolve against laid-out bounds and return 0 before layout (that bug shipped briefly as a square selection frame in +72); `getStrokeWidth()` is a plain int and safe to capture (used for the unselected 1dp style stroke). Straight separator lines are drawn by `main/MainSeparatorDecoration.java` (offsets reserve the gap — right of every non-last column, below every row; rows keyed by shared decorated top, line at the tallest cell's bottom). Widths in `fonts/SeparatorPrefs.java` (`shiroikuma_separators`, dp floats, default 0.5, 0 = none, max 8); colours are `ColorPrefs.SEPARATOR_H`/`SEPARATOR_V` (default yellow). Settings UI: "Main list separators" group at the end of **UI colors & fonts** (`FontsPreferences.buildSeparatorElement`, `view_separator_element.xml` — slider in 0.5dp steps + standard colour row). Live refresh via `SeparatorPrefs.consumeChanged()` in `MainActivity.onResume` → `MainSeparatorDecoration.reload` + `invalidateItemDecorations` (also on `ColorPrefs` changes, since the colours live there). |
| Anti-snooping tab (`snooping/` + app details) | Fork tab **Snooping / 盗み見**, displayed **second, right after App info**, listing every privacy-invasive capability of the app that ADB/Shizuku can *actually* switch off. **Display order is decoupled from the property id**: `AppDetailsFragment.SNOOPING` is **13** (appended, never inserted — those ids key the view model's per-property state) and `AppDetailsActivity.TAB_PROPERTIES` maps tab position → property; that array and `R.array.TAB_TITLES` must stay the same length and order. Catalogue = `snooping/SnoopingCatalog.java`: entries name their app-op by **AOSP name** (`"FINE_LOCATION"`), never by numeric code (codes are renumbered between releases), resolved against the running platform by walking `0.._NUM_OP` and asking `opToName`. Each entry's `id` is a **stable wire key** persisted + exported — **never rename one**. Three tiers, all computed on-device, never hardcoded: **requested** (app declares the permission, or the op is at a non-default mode), **ungated** (`AppOpsManagerCompat.opToPermission(op) == null`, i.e. the app can use it without declaring anything — screen capture, clipboard, assistant reads, VPN, background run) — both shown by default — and **not-requested** (permission-gated, app never asked), hidden behind ⋮ → *Show all* as a pre-set that lands when a future update starts asking. **LANDMINE — `opToSwitch`, verified on-device 2026-07-27:** `AppOpsService` runs every write through `AppOpsManager.opToSwitch(op)` **before storing it**, so an op whose switch op is a *different* op has **no slot of its own** — the write silently lands on the controlling op and the row can never move (for us, for `adb`, or for root). Proof: clearing `COARSE_LOCATION` then writing `GPS = ignore` produced `COARSE_LOCATION: ignore`, while `GPS` still read "No operations. Default mode: allow"; same for `MONITOR_HIGH_POWER_LOCATION`. `SnoopingCatalog.isIndependentlySettable(op)` therefore drops **any** op where `opToSwitch(op) != op` — a rule, not a list, so a capability merged on one Android version and split on another needs no maintenance. **LANDMINE — stored mode ≠ enforced mode:** the per-op entry from `getOpsForPackage` is *absent* until something writes it, so a row built on it defaults to `opToDefaultMode` and claims "Allowed" for capabilities the system already denies. `AppDetailsSnoopingItem.refreshEffectiveMode` reads `checkOperation` (which resolves uid modes + op switching) instead; `isAllowed()` prefers it and falls back to the stored mode only when the platform refuses to answer. Same gap broke *writes*: `PermUtils.setAppOpMode` returns early when `checkOperation` already equals the target, which silently swallowed `PHONE_CALL_MICROPHONE`/`PHONE_CALL_CAMERA` in +6 — so ungated/pre-set rows now call `appOpsManager.setMode` **unconditionally** (rows with a real modifiable permission still go through `AppDetailsAppOpItem.allowAppOp`/`disallowAppOp`, which moves permission + op + flags together). A **not-requested** row must move the **op alone** — granting/revoking a permission the app never declared throws and would lose the pre-set. **LANDMINE — uid mode vs package mode, measured on-device 2026-07-27 (+8):** `AppOpsManagerCompat.setMode` writes the **uid** mode on Android M+ (`setUidMode`) while `adb shell appops set <pkg> <op> <mode>`, other tools, and older builds of this fork write the **package** mode — a different slot. `AppOpsService` **deletes** an entry whose mode equals `opToDefaultMode` instead of storing it, so *un-blocking* an op whose default is `allow` (e.g. `PHONE_CALL_CAMERA`) cleared a uid entry that never existed while the package-level `ignore` kept winning: `checkOperation` still answered `ignore`, the switch stayed off, nothing threw, and the decision was recorded anyway → the row read **"Blocked · saved: allow"**. Proof: `appops set --uid … PHONE_CALL_CAMERA ignore` adds `PHONE_CALL_CAMERA: mode=ignore` under `Uid u0a…:`; writing `allow` (its default) makes that line vanish with the package entries untouched. Fix = `AppOpsManagerCompat.setModeBothLevels` (uid first, then package best-effort — correct in all four default/target combinations), plus `AppDetailsSnoopingItem.repairIfUnchanged` (forces the package entry when the enforced mode did not move, so the permission path keeps its `MODE_FOREGROUND` nuance unless it demonstrably failed). **Store only departures from the default (白い熊, +13).** `AppDetailsSnoopingItem.defaultAllowed()` resolves the untouched state from `opToDefaultMode` (`MODE_ALLOWED`/`MODE_FOREGROUND` → allowed, `IGNORED`/`ERRORED` → blocked, `MODE_DEFAULT` → the linked permission decides, so dangerous ⇒ blocked, otherwise **null = unknown**); `isDefaultState(allowed)` gates the write, and a toggle back to the default **`clearSetting`s** the row rather than storing it — otherwise the enforcer would keep re-asserting a no-op and it would travel in every export as noise. `blockAllSnooping` must do the same *and* clear such ids explicitly, because `SnoopingPrefs.setSettings` only merges. **Null means "store it"** — a redundant record is harmless, a dropped one is not. The row's status line therefore says **nothing** about the stored decision (it would only restate the switch); what marks a row instead is **one accent colour driving both the card stroke and the switch** (+14/+15): **red `#ff0028` whenever the capability is allowed** (the app can do it right now), **yellow `ForkThemeUtils.getTextColor()` when it is blocked but the platform default is allow** (you closed it), **grey = the card's own `defaultStrokeColor` when it is blocked and blocked is the default** (nothing to look at). The **3dp stroke is drawn only when `isChangedFromDefault()`** — an allowed-by-default row gets a red switch but no frame. The switch takes the accent on `setThumbTintList` + `setTrackDecorationTintList` (dot + outline) with `setTrackTintList` transparent so it stays hollow in every state; `MaterialSwitch` (material 1.13) is the only widget with `trackDecoration`. The "Allowed" pill is a **filled blood red `#6E0B14` with near-white `#FFD9DC` text** (a wash was too bland for the state that matters); "Blocked" stays a green wash — the only tell for the case that has no other one, a capability that is ON by default and off only because you turned it off. Capture `card.getStrokeColor()`/`getStrokeWidth()` in the ViewHolder ctor and restore them in the else branch (recycling); both are plain fields, unlike `getRadius()`. **Never record a decision a write did not achieve:** `matchesRequestedState(requested)` re-reads `checkOperation` and both `setSnoopingAllowed` and `blockAllSnooping` skip the `SnoopingPrefs` write when it disagrees (it returns `true` when it *cannot* tell, so only a positive contradiction counts as failure). The **status chip is the handle for the saved decision** — tapping it (only when one exists) opens the same dialog as a long-press, with *Forget this setting*. **Inclusion rule (白い熊, +10/+11): the page shows only what is snooping now, or what can be *made* to snoop.** Three filters serve it. **(1) Reachability — `snooping/SnoopingReachability.java` (+11), the cheap one, and the one to extend first:** judged from the app's own manifest on every load, no writes and nothing persisted, so an update that adds the missing declaration brings the row back by itself. Per-capability prerequisites, and *only* where unambiguous: `media_*_read` → a `READ_MEDIA_*`/`READ_EXTERNAL_STORAGE`/`MANAGE_EXTERNAL_STORAGE` declaration; `microphone_hotword`+`microphone_call` → `RECORD_AUDIO`; `camera_call` → `CAMERA`; `sms_write` → any SMS permission (no SMS permission ⇒ cannot hold the default-SMS role); `device_identifiers` → system/privileged or `READ_PRIVILEGED_PHONE_STATE` (signature-only since Q); `accessibility`/`vpn`/`assist_*` → a `<service>` whose `android:permission` is the matching `BIND_*` (assist also accepts an `ACTION_ASSIST` activity, found via `queryIntentActivities`). **Fails open everywhere** — unknown id, or any thrown lookup ⇒ reachable. **LANDMINE (cost build +11):** `PackageInfo.services` is `null` for **two** different reasons — the caller omitted `GET_SERVICES`, *or* the app declares no services at all. The first is "no data" (fail open), the second is the strongest possible evidence of unreachability (fail closed), and a `PackageInfo` cannot tell you which. Treating both as "no data" left every service-gated row on the page for exactly the apps that most deserved to lose them (8 rows instead of 4 for a zero-service app). `SnoopingReachability.services()` therefore **re-queries with `GET_SERVICES | MATCH_DISABLED_COMPONENTS` when it sees a null** and only a *thrown* query counts as unknown. Same trap applies to `activities`/`receivers`/`providers` if a future rule uses them. Capabilities with no manifest gate at all (`PROJECT_MEDIA`, `READ_CLIPBOARD`, `RUN_*_IN_BACKGROUND`) can never be hidden this way. For a zero-permission app this drops 12 of the 16 ungated rows. **(2) Learned marks — `SnoopingImmovable`, the backstop** for refusals no manifest explains: `isModifiable()` is a privilege check, not a per-op one, so a row that is blocked and refuses to move was still rendered as a dead switch. The platform cannot be asked in advance (it accepts a write and discards it), so it is learnt: when a *turn-on* write leaves `checkOperation` unchanged, `snooping/SnoopingImmovable.java` marks `pkg`+`id` and `SnoopingResolver` drops the row. **A mark only hides a row that is currently BLOCKED** — a stale mark can therefore never conceal actual snooping, only omit a safe row. Own prefs file `shiroikuma_snooping_immovable.xml`, listed in `SettingsBackupManager.EXCLUDED_PREFS` (a fact about this phone's app-ops behaviour, not a decision — carrying it to another phone would hide a capability that is movable there). Self-healing: `SnoopingInstallReceiver` calls `clearPackage` on `PACKAGE_ADDED` and `clearAll` on `MY_PACKAGE_REPLACED` (a new build may write app-ops differently — +9 did), and *Show all* reveals them. **(3) Writability of a permission-only row — `SnoopingResolver.canWritePermission`, measured on-device 2026-07-28 (+17).** `location_background` (`ACCESS_BACKGROUND_LOCATION`) is the catalogue's only `Entry.perm`: no app-op exists for it on any release, so the permission itself is the lever. `AppDetailsPermissionItem.modifiable` = `PermUtils.isModifiable`, which asks about **our** privileges and the protection level and says nothing about the app the write lands on — and `PermUtils.revokePermission` branches on `supportsRuntimePermissions(applicationInfo)` (`targetSdkVersion > 22`): a **legacy app takes the compat branch, which can only act through the app-op** and never clears the grant itself, so for a permission with no op the branch does nothing, `permission.isGranted()` is still true when `persistChanges` runs, and it promptly **re-grants** what you asked it to revoke. Proof: `com.polyclock` (複数時計, target SDK 22) → `ACCESS_BACKGROUND_LOCATION: granted=true, flags=[REVOKED_COMPAT|REVOKE_WHEN_REQUESTED|…]`, switch immovable for us and for anything else. So a permission-only row is dropped unless `PermUtils.supportsRuntimePermissions(app) || permission.affectsAppOp()` — the platform's own rule, so a modern app keeps its working switch and an app that raises its target SDK gets the row back with no maintenance. Paired fix: `matchesRequestedState` used to return `true` for *every* permission-only row (op-less ⇒ "no evidence"), so a refused revoke was recorded as a success; `AppDetailsSnoopingItem.refreshEffectivePermission` now re-reads `PermissionCompat.checkPermission` after the write — **and consults the op when `affectsAppOp()`**, because a legacy compat-revoke deliberately leaves the grant in place and only moves the op, which a bare `checkPermission` would misread as a failed write. The "needs no permission" suffix keys off `capability.isUngated()`, **not** the tier: keying it off `TIER_UNGATED` made the label disappear the moment an op got an explicit non-default mode (which promotes the row to `TIER_REQUESTED`), so blocking a row silently changed what it claimed about itself. Store: `snooping/SnoopingPrefs.java` → own prefs file `shiroikuma_snooping.xml`, key `pkg:<packageName>`, value `id=0/1` pairs; absence is a real third state (*not managed*), unknown ids survive read-modify-write. Because it is its own `shared_prefs/*.xml` it flows into Export/Import as `Category.SNOOPING` (wire id `snooping`) with no serialisation of its own. Replay: `snooping/SnoopingEnforcer.java` — `SnoopingInstallReceiver` (manifest-registered `PACKAGE_ADDED`, one of the implicit broadcasts still delivered on O+, plus `MY_PACKAGE_REPLACED`) and a sweep from `Ops.setAuthenticated(ctx, true)`, the one point where privileges are settled; without privileges nothing is dropped, only deferred to the next launch. `snooping/SnoopingResolver.java` is the **single** place deciding what the tab shows, shared with the enforcer so replay can never disagree with the UI. **Beyond app-ops (白い熊, +18).** The ops turned out to be the easy half; the capabilities that matter most are gated elsewhere, so a row now has **three possible mechanisms** and `SnoopingResolver` picks one: op → permission-only → **lever**. A lever (`snooping/lever/`, registry `SnoopingLevers` keyed by the same stable wire id) answers `isApplicable`/`isModifiable`/`isAllowed`/`setAllowed`/`defaultAllowed`/`detail`, and `setAllowed` must **return whether the platform actually did it** — the caller records nothing otherwise. Five exist: **`network_internet`** (per-uid net policy; `INTERNET` is install-time so it can never be revoked — the *strongest available* reject bits are discovered by reflecting over `NetworkPolicyManager`'s own fields, strongest-first `POLICY_REJECT_ALL` → `POLICY_NETWORK_ISOLATED` → `REJECT_WIFI+REJECT_CELLULAR` → AOSP's `POLICY_REJECT_METERED_BACKGROUND`, and the row's detail chip **names the rung** so the switch never claims more than it does), **`accessibility_service`** + **`notification_listener`** (the colon-separated `Settings.Secure` lists — `enabled_accessibility_services` with `accessibility_enabled` following it down, and `enabled_notification_listeners`; the `ACCESS_ACCESSIBILITY`/`ACCESS_NOTIFICATIONS` **ops are not these gates** and are kept as separate rows), **`assistant_role`** (`cmd role {get,add,remove}-role-holder android.app.role.ASSISTANT` **and** the older `Settings.Secure` `assistant`/`voice_interaction_service` pair — both are read, blocking clears both), **`battery_exemption`** (`DeviceIdleManagerCompat`; both compat methods return `true` for cases they did not change, so the state is always re-read). **LANDMINE — secure settings cannot go through `Settings.Secure.putString`:** that runs in *our* process under *our* uid and 応用管理 holds no `WRITE_SECURE_SETTINGS` (`ActivityManagerCompat.startActivityViaAssist` has to `requireSelfPermission` it), so every write goes through `Runner` (`settings put --user <id> secure …`, `settings delete …` for an empty list — `put ''` stores a literal empty string on some builds). Reads use the in-process `Settings.Secure.getString` only for **our own user** and the shell for any other. A failed *turn-on* **never marks a lever row immovable** (`item.lever == null` guard in `setSnoopingState`): an a11y service or assistant role we could not restore is one the user can still restore from Settings, so hiding it would hide something that may come back. **Tri-state (`snooping/SnoopingState.java`).** Rows are now `BLOCKED=0`/`ALLOWED=1`/`FOREGROUND=2` — the numbers are the **wire format** in `shiroikuma_snooping.xml`, so 0/1 must keep their meaning forever and an unknown value decodes to BLOCKED. `SnoopingPrefs` therefore stores `Map<String,Integer>`, and the item exposes `getState`/`setState`/`supportedStates`/`nextState`; **tapping a card advances to the next state it supports**, long-press lists them explicitly. `supportsForeground()` is restricted to op rows with a **dangerous runtime permission** — `AppOpsService` will happily store `MODE_FOREGROUND` for any op, but only where the framework consults the uid's process state does it mean anything, and a third position that silently behaved like "blocked" is worse than not offering it. When the permission path is taken, `allowAppOp` puts the op at `MODE_ALLOWED` first, so FOREGROUND **re-narrows the mode afterwards** at both levels while leaving the grant in place. The switch is on for both allowed states (a switch has two positions and the row has three); the state is read from the **theme yellow** pill/stroke — `ForkThemeUtils.getTextColor()` via `statusColorForeground()`, **not** the old hard-coded amber `#FFC24B` (白い熊, +56: the fork has one yellow, and it is the configurable theme's). It therefore shares a colour with "blocked by us", and the two are told apart by the pill's words — a narrowed row names its own rung, e.g. "No background mobile data". **Components (`snooping/SnoopingComponents.java`).** One place resolves capability id → the `ComponentName`s behind it (a11y, NLS, VPN, voice-interaction + `ACTION_ASSIST` activities); the long-press *Disable / enable the component* action drives `PackageManagerCompat.setComponentEnabledSetting(DISABLED_USER)` — the blunt instrument and the only answer for a capability with no op, deliberately **not** recorded in `SnoopingPrefs` (component state is the Blocking feature's territory and survives on its own). `SnoopingReachability` now delegates its `services()` to this class, and the lookup is **memoised in a `WeakHashMap` keyed by the `PackageInfo` instance** — one resolve pass would otherwise re-query the package manager once per component-bearing capability. **Catalogue completeness:** ⋮ → *Ops not in the catalogue* (`SnoopingCatalog.missingOpNames()` + `AppDetailsViewModel.getUncataloguedOps`) walks `0.._NUM_OP`, drops what `isIndependentlySettable` rules out and what is already listed, and shows/logs the rest with this package's current mode — so extending the catalogue is reading a list rather than remembering one. **Group order is by consequence, not subsystem** (`Group` enum order *is* render order and nothing persists it): NETWORK → ACCESSIBILITY → SCREEN_WATCHING → LOCATION → MIC_CAMERA → MESSAGING_CALLS → PERSONAL_DATA → STORAGE_MEDIA → NEARBY → BACKGROUND. **All-network block (白い熊, +19).** The metered-background policy is the *narrow* rung — measured on the Mate XT, `Metered ifaces: {rmnet0}`, so on WiFi it cannot fire at all — so `network_internet` is now **three-state**: ALLOWED → `FOREGROUND` = the net policy (labelled **"No background mobile data"**, never "Only while in use" — a lever names its own states via `SnoopingLever.stateLabelRes`, because the generic label would misdescribe it) → BLOCKED = a **per-uid firewall DENY on every interface**, via `ConnectivityManagerCompat.setUidFirewallRule` (the compat class and the `IConnectivityManager` stubs already existed upstream). **Chain = `FIREWALL_CHAIN_OEM_DENY_2`**: the AOSP chains (dozable/standby/powersave/restricted/low-power-standby) are driven by `AppStandbyController`, which rewrites their rules on every bucket change and would silently undo ours; the three `OEM_DENY` chains exist for exactly this and AOSP never touches them (`_2` rather than `_1` on the assumption an OEM helping itself takes the first). `setFirewallChainEnabled(chain, true)` is asserted on every block — idempotent, and unreadable before Android 14. **Privilege: shell holds `NETWORK_SETTINGS`** (verified on-device 2026-07-28: `NETWORK_SETTINGS`/`MANAGE_NETWORK_POLICY`/`CONNECTIVITY_INTERNAL` yes, `NETWORK_STACK` no), which is what `ConnectivityService.setUidFirewallRule` accepts. **LANDMINE — the rule is write-only on Android 13:** `getUidFirewallRule`/`getFirewallChainEnabled` are U+, and no `dumpsys` shows chain membership, so the tab's "always re-read what the platform enforces" discipline has nothing to ask. `snooping/NetBlockState.java` (own prefs `shiroikuma_netblock.xml`, in `EXCLUDED_PREFS` — device-local, and uids do not travel) records writes that **returned without throwing**, which for *this* API is real evidence (it throws `SecurityException`/`ServiceSpecificException` on refusal, unlike `AppOpsService`, which accepts and discards); Android 14+ asks the platform instead. It stores the **uid** alongside the package because a reinstall changes it, and unblocking clears the rule for the *recorded* uid as well as the current one, or a stale DENY outlives the app and lands on a recycled uid. The policy rung is applied *alongside* the firewall block, not instead of it, so a flushed chain still leaves the weaker protection. **Ops added from the on-device report** (+19, the `missingOpNames()` feature paying for itself — 50 uncatalogued on Android 13): `ACTIVATE_PLATFORM_VPN`, `MANAGE_IPSEC_TUNNELS`, `BIND_ACCESSIBILITY_SERVICE` (a second, independent gate beside the secure-settings list), `LOADER_USAGE_STATS`, `RECORD_INCOMING_PHONE_AUDIO`, `RECORD_AUDIO_OUTPUT`, `SMS_FINANCIAL_TRANSACTIONS`, `USE_ICC_AUTH_WITH_DEVICE_IDENTIFIER`, `CALL_PHONE`, `QUERY_ALL_PACKAGES`, `INTERACT_ACROSS_PROFILES`, `NO_ISOLATED_STORAGE`, `CHANGE_WIFI_STATE`. The report now filters op names starting with `deprecated` (the framework keeps retired codes as placeholders to hold the numbering). **LANDMINE — `checkOperation` never answers `MODE_FOREGROUND` (白い熊, +20, cost a build):** `AppOpsService` runs the stored mode through `UidState#evalMode` before returning it, resolving foreground into ALLOWED or IGNORED **according to the app's process state at that instant**. So the enforced mode is the right source for "can it do this right now" and the wrong one for "what did we set": a row narrowed to foreground read back as BLOCKED whenever the app happened to be backgrounded, `matchesRequestedState(FOREGROUND)` was permanently false, and — because the old guard treated any failed *allowed-ish* request as "will not turn on" — the row was marked `SnoopingImmovable` and **vanished from the page** with the "cannot be turned on here" toast. Two fixes, both needed: `getState()` reads the **raw stored mode** (`opItem.getMode()`, uid entry then package entry via `getOpsForPackage`) and reports FOREGROUND from it *before* consulting `checkOperation`; and the immovable mark now requires `state == ALLOWED && !isAllowed()` — a refused **narrowing** says nothing about whether a capability can be turned on. (A stale mark is self-healing: `SnoopingInstallReceiver.clearAll` on `MY_PACKAGE_REPLACED` wipes them when this app is updated.) **Dialogs: `utils/ForkDialog` is the shared entry point** — `builder(ctx)` (yellow-on-black overlay pinned explicitly) + `present(builder)` (create → show → border) or `bordered(dialog)` for the call sites that do not own the `show()`, i.e. `DialogFragment.onCreateDialog` and dialogs whose buttons are re-wired after `create()`. `UIUtils.presentWithYellowBorder`/`yellowOnBlackDialog` are the older equivalent pair, still used by the snooping/battery screens; the monitor keeps its own private copy. **The border is never inheritable from a theme**: `MaterialAlertDialogBuilder.create()` rebuilds the window background as a stroke-less `MaterialShapeDrawable`, so `UiUtils.applyForkDialogBorder(dialog)` (driven by the `forkDialogBorderDrawable` attr on the dialog overlay, and a no-op wherever the attr is unset) must run afterwards — which is why **every libcore builder (`Searchable*`/`TextInput*`/`Scrollable`) is already bordered while a plain `new MaterialAlertDialogBuilder(ctx).…show()` is not**, no matter how fork-themed the host activity is. That asymmetry is what made the backup **delete-confirmation** dialog (and ~35 others) render borderless until +55 swept them; when adding a dialog to a yellow-on-black screen, route it through `ForkDialog` or it will silently be the odd one out. **Component disabling was tried and removed (白い熊, +18 → +23).** A row action, then a fourth state `COMPONENT_OFF = 3`, then nothing: **LANDMINE — shell cannot change component state at all** (measured on-device 2026-07-28). Since Android O the platform refuses it for anything that is not a **test-only** app; `pm disable <pkg>/<cls>` answers `SecurityException: Shell cannot change component state for … to 2`. Upstream's `SelfPermissions.canModifyAppComponentStates(userId, pkg, testOnly)` already encodes exactly this — consult it before building anything on `setComponentEnabledSetting`. The whole feature is gone from `SnoopingState`, `AppDetailsSnoopingItem`, `SnoopingResolver`, the fragment and the strings; wire value `3` is retired and decodes to `BLOCKED`. `SnoopingComponents` stays, minus its `forCapability` entry point, because `SnoopingReachability` and the levers still need the component lookup. **Also not a lock even where it works:** an app **can re-enable its own components** programmatically (`PackageManagerService.setEnabledSetting` permits a same-uid caller), so the strongest per-component action would never have been the hard block it looked like. **LANDMINE — a permission-backed op's mode is NOT independently settable (白い熊, +22, measured on-device 2026-07-28).** `PermissionPolicyService` re-derives the op mode from the permission grant and re-asserts it, so a direct write to such an op is accepted and thrown away. Proof on `org.localsend.localsend_app`: `cmd appops set <pkg> READ_EXTERNAL_STORAGE ignore` → still "No operations. Default mode: allow"; `set --uid … foreground` → stored as **`ignore`**; and on a package whose `CAMERA` permission is denied, a `--uid … CAMERA allow` write was back to `ignore` within one command. This is *why blocking works anyway* — `AppDetailsAppOpItem.disallowAppOp` revokes the permission and the policy service then agrees with us — and why `AppDetailsSnoopingItem.supportsForeground()` is now **hard-coded false for op rows**: `MODE_FOREGROUND` can never be written onto a permission-backed op. The platform's real "only while in use" is expressed by the **permission**, not the op (grant `ACCESS_FINE_LOCATION` without `ACCESS_BACKGROUND_LOCATION` and the policy service sets `MODE_FOREGROUND` itself) — which is the `location_background` row this tab already has; camera and microphone have no such permission pair at all, their "while using the app" being enforced by process capabilities with nothing to set. **Only the network lever keeps a middle rung**, because its middle state is a genuinely different mechanism (a per-uid policy) rather than an op mode. The `FOREGROUND` branches in `applyCapabilityState`/`modeFor` are kept — they are correct if the platform ever grows a settable foreground — and only the *offer* is withdrawn. Note the corollary for **ungated** ops (no permission behind them — clipboard, screen capture, background running): those have no policy service to fight and direct mode writes work, which is why those rows have always behaved. |
| **Accessibility is shown unconditionally** (白い熊, +68) | `ACCESS_ACCESSIBILITY` used to be gated in `SnoopingReachability` on the app declaring a `BIND_ACCESSIBILITY_SERVICE` service, and measurement supported the rule (on the Mate XT, all ten packages that had ever exercised the op declared such a service, and six of the seven with the op blocked declared none — those blocks predate +11). It is off anyway: this is the capability that subsumes every other on the page, the op costs nothing to block, and a manifest rule that is right about today's APK still hides the row from the app that adds the service in its next update. Being wrong here is not symmetric. **Catalogue additions in the same build:** `install_packages`/`delete_packages` (`REQUEST_INSTALL_PACKAGES`/`REQUEST_DELETE_PACKAGES`, Personal data — their permissions are `normal`, so `PermUtils` takes the read-only branch and moves the **op alone**, never attempting to revoke an install-time permission), `sip_calls` (`USE_SIP`, Messages & calls), and `read_logs` — the **second permission-only row** after `location_background`, working for the same structural reason: `READ_LOGS` is `signature\|privileged\|**development**`, and the development flag is what makes it revocable (it builds as a `DevelopmentPermission`, `readOnly=false`, so `PermUtils` acts on the grant itself). |
| **Only movable rows are listed** (白い熊, +56) | `SnoopingLever` has an `isModifiable(packageInfo, userId)` overload beside the privilege-only `isModifiable()`, and `SnoopingResolver:120` asks **that** one. Privileges are not the whole question: a capability can be modifiable in general and pinned for one package. Judged live on every load, never cached or persisted, so the same app on another phone keeps its working switch and an OS update that unpins it brings the row back by itself. **LANDMINE — there are THREE doze whitelists, and only two have per-package removals (measured 2026-08-01).** `dumpsys deviceidle whitelist` names them in its own output: `user,<pkg>`, `system,<pkg>`, `system-excidle,<pkg>`. `removePowerSaveWhitelistApp` touches only the **user** list; `removeSystemPowerWhitelistApp` (added to the `compileOnly` `IDeviceIdleController` stub, along with `restoreSystemPowerWhitelistApp` and the three `get*PowerWhitelist` readers) handles the **system** list and was verified working on EMUI, restoring cleanly. The **except-idle** list has **no per-package removal anywhere** — `cmd deviceidle except-idle-whitelist` offers only `+` (add), `=` (check) and `reset`, and no binder call exists — so an entry there cannot be cleared by us, by `adb`, or by root. A Motorola razr 40 ultra ships `com.android.vending` exactly that way, which is what surfaced the whole rule: the row said "Could not change this capability" truthfully, forever. `DeviceIdleManagerCompat.isExemptionRemovable(pkg)` decides from **list membership** rather than from `isPowerSaveWhitelistExceptIdleApp`, whose reach varies by release — a wrong answer here hides a switch that works, so it fails **open** on any unreadable list. `BatteryExemptionLever` and the battery panel's doze switch both consult it. |
| **Each row says when it was last used and last denied** (白い熊, +99) | `snooping/SnoopingActivityTimes.java` + the `@id/snooping_activity` chip, bound by `AppDetailsSnoopingFragment.activityText`. The numbers are `AppOpsService`'s own `lastAccessTime`/`lastRejectTime`, which the row **already held** — `AppDetailsSnoopingItem.opItem` carries the `OpEntry` and upstream's App-ops tab has rendered the same pair for years (`AppDetailsPermissionsFragment:630`). No new binder call, no new permission; `forItem()` is field access and safe at bind time. It is deliberately **not** snapshotted on the item, because a write calls `AppDetailsAppOpItem.invalidate`, which replaces the entry, and a cached copy would show the times from before the write. **The feature is the tri-state, not the timestamps** (白い熊's ask): every segment is a time, **never**, or **not recorded**, because a blank line reads as "clean" and for the interesting cases that would be a lie. **LANDMINE — an empty op-entry map and a failed query are the same map.** `SnoopingResolver` swallows the `getConfiguredOpsForPackage` throw, so without a flag every row of an unreadable package would claim "never used"; `AppDetailsSnoopingItem.opStateReadable` (set only on the success path, default **false**) is what separates them. **LANDMINE — a denial is recorded only when the app-op is the gate that fires.** `lastRejectTime` moves when the app reaches `noteOp` and gets `MODE_IGNORED` — true for the **ungated** ops (clipboard, screen capture, background run), which we block by writing the mode. But a permission-backed capability is blocked by revoking the permission (+22), and the permission check then refuses **before** `noteOp`: nothing is ever recorded, however often the app tries. Reporting that as "never denied" would look like a broken feature, so it renders as *"Denials not recorded (stopped at the permission)"* — reason `REASON_PERMISSION_GATE`, which also covers a not-requested pre-set row. A recorded reject time always wins over the reason (a legacy compat-revoke leaves the grant and moves the op, which is exactly the case that *does* record). Use has no such asymmetry: a call that never passes the permission check never happened, so "Never used" stays a fact. Absence of the whole `OpEntry` is likewise evidence, not a gap — `AppOpsService` creates the `Op` on the first `note`/`setMode` and prunes it only when it holds neither a mode nor access data. **Background-aware:** `hasBackgroundTimeSplit()` gates on **API 28+**, below which `OpEntry` carries one undifferentiated time and the compat layer answers every question with it — a "(background)" marker there would be invented rather than measured. A recorded denial is the only part drawn in the theme yellow: it is the one line you would act on. Lever and permission-only rows say *"No usage record for this kind of capability"* rather than nothing at all. Explained in the legend (⋮ → *What the marks mean*), not in a new surface. |
|---|---|
| **Tabs are three views of one state** (白い熊, +69) | **LANDMINE — a tab is loaded once and never again.** `AppDetailsActivity.loadTabs()` loads *every* property on package change; paging between tabs reloads nothing, and a write notifies only the row that issued it. So blocking a capability on **Snooping** left the **App ops** tab still showing the old mode — and that is not merely stale reading: `AppDetailsViewModel.setAppOpMode(item)` picks its direction from `appOpItem.isAllowed()`, i.e. from the stale mode, so the next toggle there wrote **the opposite of what its switch appeared to do**. Fix = `mStateEpoch`, an `AtomicInteger` bumped by every write that changes what an app may do (`setSnoopingState`, `blockAllSnooping`, `setAppOp`, both `setAppOpMode`s, `togglePermission`, `revokeDangerousPermissions`, `resetAppOps`); `AppDetailsSnoopingFragment` and `AppDetailsPermissionsFragment` remember the epoch they rendered and re-read in `onResume` **only when it moved** (ViewPager2's `FragmentStateAdapter` drives RESUMED on the selected page, which is what makes onResume the right hook). Add the bump to any new write here, or its tab silently drifts out of date. |
| **Drift is shown, not just detectable** (白い熊, +69) | A remembered decision that no longer matches the enforced state (`AppDetailsSnoopingItem.isDrifted()`) had **no UI at all** — the switch and the pill both report the live state faithfully and so *hide* the disagreement. Now: the remembered box round the switch turns **red** (`CHANGED_STROKE_ALLOWED`) instead of theme yellow, and the status pill appends `snooping_status_drifted` — "you set: <state>". The pill's standing rule (say nothing about the stored decision, +13) holds only while the two agree; when they differ, the stored decision is the one thing the row cannot otherwise say. State labels go through the new `stateLabel(context, item, state)` helper, which keeps the `stateLabelRes == 0` trap (0 means "use the generic label", and `getString(0)` throws). Documented in the legend, not in a new surface. |
| **A stored `MODE_FOREGROUND` outlives its permission** (白い熊, +69, measured on `com.elink.fittrackhealth.pro`) | That app has `ACCESS_COARSE_LOCATION: granted=false, flags=[USER_FIXED]` while the **uid** op `COARSE_LOCATION` was still at `foreground` — `PermissionPolicyService` usually syncs such an op down to `ignore` and here plainly had not. `getState()` reads the raw stored mode first (the +20 rule, needed so a narrowing is visible at all), so the row claimed **"Only while in use" with its switch ON for an app that cannot use location at all** — and it could not be moved: asking for BLOCKED re-revoked an already-revoked permission, nothing changed, and `matchesRequestedState` reported failure for ever. A revoked permission is no access whatever the op says, so the permission now wins — but **only when the app actually declares it** (`opItem.appContainsPermission`): a `TIER_NOT_REQUESTED` row is built with `isGranted=false` by construction, and reading that as "blocked" would mislabel every pre-set row on the page. |
| **The migration kit — a clean phone becomes this phone** (白い熊, +154) | `appdata/self/MigrationKit.java`. **The problem is circular**: restoring 応用管理's own state through the app backup needs Shizuku or ADB first; those need 白い熊 雫; finding 雫 needs the app configured; configuring it needs the restore. So the bootstrap rides on the **settings export**, which needs no privileges either to write or to read back, and on two plain APKs a file manager can install. Written to the backup root — `shiroikuma-oyokanri_<version>_arm64-v8a.apk`, `shiroikuma-shizuku_<version>_arm64-v8a.apk` (雫's `sourceDir` is world-readable, so its copy costs no privileges), `shiroikuma-oyokanri_state.zip` (every category, **never encrypted** — a fresh install has no key) and `shiroikuma-oyokanri_migration.json`, the only file the scan identifies a kit by (a kit written by +154 used `oyokanri-migration.json`; that name is still recognised when finding one and is removed on the next write). Refreshed from `BatchOpsService.sendResults` after **every** backup run, not only when 応用管理 backs itself up: a kit older than the archives beside it restores the wrong phone. **Pruning is 白い熊's explicit decision (2026-09-06)**, against the standing never-delete-a-build rule, and happens only after the new copy's length is verified against the source, so a failed copy can never leave neither. **LANDMINE — it must NOT depend on the previous marker's record (+156).** It did, and the marker rename in +155 promptly took that record away: `readMarker` looked only for the new name, found nothing, pruned nothing, and then deleted the legacy marker that held the answer — leaving `+154` orphaned beside `+155`. It now sweeps for **this kit's own deposit shape** for the package being deposited (the package slug, anything, then the ABI suffix), which self-heals whatever the marker says and can match neither the state archive nor the marker; the recorded name is still honoured as a second pass. **Discovery**: the configured backup directory first, then the external-storage root and **two levels** under it (the real one is two deep, `〇/979`) plus every `/storage/*` volume, skipping `Android/`; it needs all-files access to see anything, so it runs after that is granted. **The offer is gated on `looksUnconfigured()`** — no profiles, no notes, no snooping decisions — rather than on "first run", so a phone that has been used never sees it whatever is lying on its storage; the one-shot flag lives in `shiroikuma_migration`, which is in `EXCLUDED_PREFS`. **Restore** imports through `SettingsBackupManager.importFrom` and the caller hard-kills, the same rule as the Export/Import panel and `PendingStateImport`. **LANDMINE — the report cannot be built from `SharedPreferences`**: this process still holds the pre-import maps, so it counts by reading the written files (profiles directory, `shiroikuma_notes.xml`, `shiroikuma_snooping.xml`, `am_backup_directory.xml`) — the same regex-on-the-XML technique `SecurityPrefGuard` uses — writes them to `files/migration_report.txt`, and `MainActivity` renders that on the next start, including how many snooping decisions name apps that are **not installed yet** (the proof the waiting settings are really there) and whether the restored backup directory exists on this phone. The phases, written into the marker: **0.** install 雫 and make it Device Owner **before adding any account** — the one step no backup can do — **1.** install us, **2.** open and accept the restore, **3.** restore the other apps, whose snooping decisions the enforcer applies as each one installs. |
| **A finding in the right-hand column is a pill** (白い熊, +149) | `main/PillSpan.java` — a `ReplacementSpan` that draws a filled, rounded pill with its own text size inside a TextView. The sibling screens' right column is three fixed TextViews of the main list's own row (`MainCardBinder`), so a headline there could only be coloured text — and the page's red `#FF0028` at 12sp on black is the one colour that genuinely cannot be read. A `RowPills` pill is a View and cannot go in a TextView; this is the same shape as a span. It sets `fm` in `getSize`, or a pill bigger than its line is clipped at both ends. 盗み見一覧 uses two: the headline in the Snooping page's own Allowed pair (`#6E0B14` under `#FFD9DC`) and, directly beneath it, the tracker count filled in the tracker red with black ink, a size up — 白い熊's rule that the trackers must be the loudest thing on the row. The headline also stopped saying "%d allowed", which never said allowed *what*: it is "%d ways to snoop". **LANDMINE — the right column's third row joined its lines through a `StringBuilder` and `toString()` (+150)**, which silently strips every span a screen put on its own lines; it is a `SpannableStringBuilder` now. It cost nothing while the lines were plain text and broke the tracker pill the moment 白い熊 asked for it to sit *below* the "only while in use" line, i.e. inside that joined block. |
| **Who is already inside the app** (白い熊, +146) | `trackers/` — the row pinned at the very top of an app's 盗み見 page, above the policy card. **Two scan depths and the difference is the point.** `TrackerScan.fromManifest` runs the app's own component names through the same Aho–Corasick index `ComponentUtils` already uses for `App.trackerCount`, and — because `AhoCorasick.search` returns the matched **indices** and `tracker_names` is parallel to `tracker_signatures` — it yields the tracker **names** for free, cheap enough for a list row. It is a **floor**: an SDK shipping no component is invisible to it. `TrackerScan.deepScan` mounts the base APK as a `DexFileSystem` (the Scanner's own trick) and walks every class name; seconds and real I/O per app, so it is per-app and on demand, cached in `shiroikuma_trackers.xml` against `lastUpdateTime` so an update re-scans by itself (in `EXCLUDED_PREFS` — derived state, and a cache that travels is a cache that lies on arrival). Hits are grouped by **name**, never by signature: several signatures share one name (AD(X) has two) and a pill per signature says the same word twice. **Colour is a measured severity rung, not a category** (`TrackerHit.rung()`): the dataset ships names and signatures and **no Exodus categories**, so a category column would have to be invented and would mostly read "Unclassified"; what the components do say is *can this thing report when I am not using the app* — a service or receiver can be started by the system (red, the page's own "it can do this right now"), a provider is built before `Application.onCreate` so it runs whenever the app runs (theme yellow), activities only need you to open their screen (grey). A second-degree tracker (the dataset's "²" prefix) keeps its rung and is drawn faded, the `RowPills` alpha language. The card is framed like the device-policy card below it, and **the yellow is painted at bind time** (`setStrokeColor(ForkThemeUtils.getTextColor())` in `TrackersViewHolder.bind`, exactly as the policy card does): the outlined card style draws a grey hairline, and the fork's ink is configurable, so no theme attribute can supply it. A pill opens `TrackerDialog`: the components it owns here, then **`TrackerAppsSource`** — every other app on the phone carrying the same tracker, as a `ScreenSource` so it inherits the sibling screens' rows, sort menu and column picker (+118) — and the Exodus report last. 盗み見一覧 gains a tracker line and a *Number of trackers* sort, reading `App.trackerCount` **from the database** rather than scanning again (the DB loader fetches all four component flags, so it is the same number the main list sorts by). **LANDMINE — `notifyPolicyChanged` assumed the policy card was row 0**, which stopped being true the moment this row was pinned above it; it now searches for the card, or a landed lock would draw its stale hollow padlock exactly as in the +75 bug. |
| **Tracker components cannot be blocked on this phone** (白い熊, +146, from the code and the +23 measurement) | `ComponentsBlocker.applyRules` returns at the top unless `SelfPermissions.canModifyAppComponentStates` passes, and that is **false for the shell uid on Android O+ for anything but a test-only app**; the other route, `canBlockByIFW()`, is `Paths.get("/data/system/ifw").canWrite()` — root only. Device Owner does not help: `DevicePolicyManager` is package-level (hide, suspend, permission grant) with no per-component API. So the tracker sheet offers **no Block button here** and prints the reason instead — the page's own rule is that a switch which cannot move is not shown, but a missing button with no explanation reads as an oversight. The pill *is* offered where `canModifyAppComponentStates` says yes (rooted phone, test-only app), asked of the platform rather than assumed. **Two existing features are quietly dead on this device for the same reason**: `ComponentUtils.blockTrackingComponents` throws away `applyRules`' return value, so the selection-toolbar *Block trackers* reports success and does nothing; and `OneClickOpsViewModel.blockTrackers` skips every non-test-only app when running as shell, so it finds nothing at all. The levers that do work on a tracker are the network one (`network_internet`) and freezing. |
| **Reopening the screen you were on** (白い熊, +146) | `main/LastScreenPrefs.java`. The +97 splash trampoline handles the easy half — while the task still exists `SplashActivity` finishes and the screen underneath resumes — but it can only resume a task that is **still there**, and EMUI drops ours while Android resets a task to its root when it is returned to after long enough away. Then the launcher builds a fresh task, `MainActivity` is created for real, and nothing in the task stack remembers 保存一覧 or an app's 盗み見 page. So it is written down: one `ActivityLifecycleCallbacks` installed from `AppManager.onCreate` records the resumed activity's own `Intent` (`toUri(URI_INTENT_SCHEME)`), and `MainActivity.reopenLastScreen` replays it — only from a genuinely fresh start (`savedInstanceState == null`, no data URI, so the `market://search` path is never hijacked) and **at most once per process**, so backing out of the restored screen leaves you on the list rather than in a loop. Returning to the main list clears the memory by itself, because `MainActivity` is recorded like everything else and recording it means "nothing to reopen". **An allowlist, deliberately** (`REOPENABLE`, class objects so a rename cannot silently disable it): this app is full of activities that must never be replayed — installer, keystore prompt, freeze-unfreeze trampoline, the progress page of a finished operation — and an intent carrying a `content://` URI would be replayed without the grant that made it work, so an intent with data is never recorded. Paired with `android:alwaysRetainTaskState="true"` on **`MainActivity`**, which is the task root because Splash is a trampoline. **LANDMINE — it must NOT be latched to once per process (+147, measured on the phone).** +146 set a static `sConsumed` on the first `MainActivity` of a process, which looked harmless and killed the feature: on EMUI a launcher tap can create a **second** `MainActivity` while the process is still alive (`dumpsys activity activities` shows it as repeating `[Main, screen, Main, screen]` pairs in one task), and every launch after the first found the latch thrown and went to the app list — exactly the bug the feature was meant to fix. Reading the record now **clears** it instead, and nothing is lost because the restored screen records itself again as it resumes. What replaces the latch as the "is this a launch?" test is **`MainActivity.EXTRA_FROM_SPLASH`**, set only by `SplashActivity.handOff()`: every other route to this window is somebody already inside the app asking for the list — `AppDetailsActivity`'s *back to main page* most of all — and reopening the screen they just left would fight them. Verified on-device: same pid, task cleared, fresh `MainActivity`, and the 盗み見 page came back. **The list underneath is not loaded while it is covered (+149)**: reopening creates this window and immediately buries it, so reading, sizing and sorting every installed app for a screen nobody is looking at merely starves the screen that IS on top of the same threads and the same privileged server. `MainViewModel.setLoadDeferred(true)` is set **before anything observes the item list** (observing is what starts the load, so the decision has to be made at the top of `onAuthenticated`, which is why the reopen intent is computed there and started at the end), and `MainActivity.kickDeferredListLoad()` runs it from `onTopResumedActivityChanged(true)` — the moment the list stops being invisible, whether because the reopened screen was closed or because there never was one. `mReopenInFlight` covers the gap where this window is briefly top-resumed before the reopened screen arrives, `onStop` clears it, and a 3 s fallback (guarded on the lifecycle actually being RESUMED, or it would fire the load underneath the very screen it was avoiding) covers a launch that never happens. Switch on the 白い熊 応用管理 UI page (*Screens*, default on); the prefs file is in `EXCLUDED_PREFS` — a remembered screen for an app another phone does not have is worse than no memory at all. |
| Dialog **and bottom-sheet** borders | Two different mechanisms, and the second exists because the first structurally cannot reach sheets. A dialog's frame replaces the **window background** after `show()` — `UiUtils.applyForkDialogBorder`, attr `forkDialogBorderDrawable`, see the ForkDialog note in the Snooping row. A **bottom sheet is not an `AlertDialog`**: it has no window background to replace, and the dialog helper's 16dp inset would be wrong anyway for something flush with the screen edge. So `UiUtils.applyForkBottomSheetBorder(View)` sets `drawable/bottom_sheet_bg_yellow_on_black` (top corners rounded only — rounding the bottom curves the stroke away and leaves two notches) as the background of the sheet's own container, called once in `CapsuleBottomSheetDialogFragment.onCreateView`, the base **every** sheet in the app inherits. **The attr `forkBottomSheetBorderDrawable` sits on `AppTheme.YellowOnBlack`, NOT on the dialog overlay** — a sheet resolves attributes from the hosting activity's theme, so beside `forkDialogBorderDrawable` it would resolve to nothing and every sheet would stay borderless. `SelectionListBottomSheet` is deliberately exempt: it is a raw `BottomSheetDialog` that already makes the M3 sheet transparent and paints its own `ForkThemeUtils` panel. |
| Fork theme vs. Material You | **LANDMINE — dynamic colour overwrites the fork palette (measured on a Motorola razr 40 ultra, Android 15, 2026-08-01).** `AppearanceUtils.onActivityPreCreated` pins `AppTheme_YellowOnBlack` for the fork's own screens and then called `DynamicColors.applyToActivityIfAvailable` **unconditionally** three lines later, which rewrites `colorPrimary`/`colorSurface`/`colorOnSurface` from the wallpaper — a fresh install rendered **white-on-green**. It hid for months because that call is a **no-op wherever the platform does not offer Material You**: on the Mate XT (EMUI) it never fired, so the bug only appeared on the first phone that supports dynamic colour. Now skipped when `isYellowOnBlackActivity(activity)`, kept for every other screen. This is also the clobber behind the dialog-theming landmine — the yellow-on-black overlay must be passed explicitly at every call site precisely because this wiped `materialAlertDialogTheme`. |
| Main-list backup column gestures | The right-hand backup column (`backupVersion`/`backupDate`/`backupTime`) behaves like the rest of the row: **simple tap → app info** (via `handleClick`, repeating the card's selection-mode guard — without it a tap here during multi-select opens an app instead of extending the selection), **long-press → the backup dialog** (`MainRecyclerAdapter.showBackupDialog`, gated only on the app being installed *or* backed up). Bound in one place, `MainRecyclerAdapter.bindBackupColumnGestures`. Before +67 the tap started a backup and the long-press went straight to restore/delete, with nothing to tell the two apart. With no backup the column shows "Back up" plus a dimmed **"long-press"** on the second of the two visible-but-empty cells that widen the hit target; that alpha is reset in the has-backup branch (recycling). |
| **One dialog holds every backup action** (白い熊, +72) | `backup/dialog/AppBackupDialogFragment.java` — what the long-press opens. It replaced a two-line chooser ("Backup", then "Restore or delete…" which opened the bottom sheet), where the one thing you came for — **the backups themselves** — was always a screen away and *Backup* was a list line pretending to be an action. Now: the app's backups as a **tick list** (base backup pre-ticked, same default as the sheet), `Restore` (exactly one ticked) / `Delete` (≥1) / `⋮` freeze-unfreeze as pills under it, and **`Back up` as the dialog's own positive button** beside Cancel. It **reuses `BackupRestoreDialogViewModel`** — same loader, same `prepareForOperation`, same `BatchOpsService` hand-off — so behaviour is identical to `BackupRestoreDialogFragment`, which stays for batch selections and app details. Four things it must keep doing: **(1)** the positive button is wired in `onStart()` via `getButton(BUTTON_POSITIVE).setOnClickListener` so a click does **not** dismiss — the view model carrying the operation dies with the fragment, and dismissing before `startOperation` would drop the op on the floor; `ForkDialog.bordered` (not `present`) is the right helper for exactly that reason. **(2)** everything is inflated from `builder.getContext()`, the yellow-on-black **overlay** context — the fragment's own context has the activity theme, and rows inflated from it lose the tick mark (the M3 multi-choice item declares it as `app:drawableStartCompat`, which only the AppCompat inflater factory honours). **(3)** row labels are formatted on a **background thread**: `BackupMetadataV5.toLocalizedString` is `@WorkerThread` because it walks the backup directory to size it. **(4)** `processPackages` runs only when `savedInstanceState == null`; the LiveData is sticky, so a rotation re-renders from what is already loaded. `Back up` honours "Skip backup method dialog" the same way the sheet's backup-only path does, and warns before overwriting only when a **base** backup exists (`BackupInfo.hasBaseBackup()`, more precise than the sheet's app-count arithmetic). |
| **“Backup older than app” filter** (白い熊, +105) | Main-list quick filter `MainListOptions.FILTER_APPS_WITH_OUTDATED_BACKUPS` (bit `1 << 17`), mapping to a `BackupOption` at the fork key **`no_current_backup`**. **It is deliberately NOT upstream's `outdated_backup`**: that one matches an app holding *any* stale backup, so an app carrying both a current backup and an older one still shows up — useless for the one job here, which is picking exactly the apps a re-backup would change. `no_current_backup` requires the app to be **installed**, to have **at least one** backup, and for **none** of them to reach the installed `versionCode`; a single up-to-date backup clears the app whatever sits beside it. No refresh hook is needed: `BackupUtils` fires `BroadcastUtils.sendDbPackageAltered` when a backup is added or deleted, so the row drops out of the filtered list as soon as the backup lands, and an app *update* arrives on the ordinary package broadcast, which re-reads the DB row the version comes from. Keys in `BackupOption.mKeysWithType` are the **wire format of saved filter profiles** — never rename one. |
| **Suspension is invisible to the main list unless we say so** (白い熊, +81) | **LANDMINE — suspending a package emits no `ACTION_PACKAGE_CHANGED`.** The main list is never polled: `MainViewModel.onResume` re-filters only when the *Running apps* filter is on, and everything else arrives by broadcast. The platform sends `ACTION_PACKAGES_SUSPENDED` (which `PackageChangeReceiver` does listen for) and it did **not** reach us on the Mate XT — so an app suspended from the Snooping card kept the state its row was built with. That is not merely a stale label: the running box is gated on `!item.isFrozen`, so a row that never learned it was frozen also kept its **orange “running” frame** for an app the system had stopped dead. Fix: every successful policy write that changes what the list shows calls `BroadcastUtils.sendPackageAltered(...)` — the mechanism the rest of the fork already uses for its own writes, and the receiver lives on the **view model**, so it is heard while another activity is on top. Add that call to any new write here. **Second landmine, same feature:** the platform records suspension **per suspending package**, so `PackageManagerCompat.suspendPackages(..., false)` as the shell cannot lift one applied under 雫's admin — the main-list snowflake would silently do nothing for ever. `FreezeUtils.unfreeze` therefore ends with a best-effort `DevicePolicyBridge.setSuspended(pkg, false)`. Display: `ApplicationItem.isSuspendedApp` is the cheap flag filled from the same live query as `isFrozen` (**not** the `isSuspended()` method beside it, which does a lazy `fetchPackageInfo()` — far too expensive per row at bind time); the row keeps the italic and gains a violet **padlock** in place of the snowflake, a **struck-through label**, and a heavier violet film (`ColorPrefs.FREEZE_SUSPENDED` / `FILM_SUSPENDED`). The suspended branch is tested **before** `isFrozen` everywhere, since suspended implies frozen. |
| **Main-list icon tap → Snooping** (白い熊, +82) | The app icon is clickable in its own right and opens that app's 盗み見 page (`handleClick(item, AppDetailsActivity.TAB_SNOOPING)`); the rest of the icon column still toggles freeze. `handleClick` takes a tab index threaded through **all** its branches — single user, wrongly-assumed-uninstalled, multi-user picker — so the tap lands on the same tab whichever route it takes; `-1` means "default tab" and is what every older caller passes. Two guards the icon must keep, both copied from the card: the **selection-mode check** (a tap during multi-select extends the selection instead of opening an app — the icon is the easiest thing to hit by accident while selecting) and `setOnLongClickListener(v -> holder.itemView.performLongClick())`, without which a clickable child swallows the long-press that drives range selection. |
| **The main top bar scrolls sideways** (白い熊, +84) | Every action on `activity_main_actions.xml` is `showAsAction="always"` (deliberately — `ifRoom` silently drops things into the overflow), and the **search field is the toolbar's custom view** at `MATCH_PARENT` (`MainActivity.onAuthenticated`, `ActionBar.setCustomView`). On a folded panel the icons therefore ate the whole bar and the search field was squeezed to its bare search-type button — it read as "the search bar disappeared". Fix: the `MaterialToolbar` now lives inside a **`HorizontalScrollView`** (`@id/toolbar_scroller`) in `activity_main.xml`. **The mechanism is the measure mode.** `HorizontalScrollView.measureChild` **ignores `lp.width` and always passes `UNSPECIFIED`**, so the toolbar is measured at its *natural* width: nothing is squeezed, and `ActionMenuView.onMeasure` sets `mFormatItems = (mode == EXACTLY)` → false → it falls back to the plain `LinearLayoutCompat` row instead of its cell-fitting pass. `fillViewport="true"` preserves the old behaviour whenever the bar *does* fit: the child is re-measured **`EXACTLY`** at the viewport width and the `MATCH_PARENT` custom view stretches as before. **LANDMINE — `MATCH_PARENT` means nothing under `UNSPECIFIED`:** `getChildMeasureSpec` turns it into `UNSPECIFIED`, and `androidx.appcompat.widget.SearchView.onMeasure` then falls back to `abc_search_view_preferred_width` (**320dp**), which on a ~430dp panel pushes every icon off-screen — i.e. the same bug, mirrored. Hence `AdvancedSearchView.setUnconstrainedWidth(px)` (fork-only; 0 = upstream behaviour), fed `R.dimen.main_search_bar_scroll_width` (240dp) from `MainActivity`. Do **not** use `SearchView.setMaxWidth` for this — it also caps the `EXACTLY` case and would stop the field stretching on the unfolded screen. Free consequences: `HorizontalScrollView` auto-scrolls to a focused child, so `focusSearch()`/the market-URI path still bring the field into view; `attachOverflowLongPress` still works, since it walks the toolbar's own children. The sibling screens (monitor, battery, usage) have **one** `always` item each and need none of this. |

## Device policy — hard locks delegated by 白い熊 雫 (`devicepolicy/`)

Everything the Snooping page did before +57 is **soft**: an op we write or a permission we revoke can
be put back by Settings, by another tool, sometimes by the app. 白い熊 made the sister app **白い熊 雫**
(`shiroikuma.shizuku`, repo `shiroikuma-shizuku`) **Device Owner**; it delegates policy scopes to us,
and a decision can then be made **hard**.

- **Two channels, and the split is load-bearing.** `DevicePolicyBridge` covers what the platform will
  delegate (`DELEGATION_PERMISSION_GRANT`, `DELEGATION_PACKAGE_ACCESS`, `DELEGATION_BLOCK_UNINSTALL`)
  and calls the **public SDK with a `null` admin** — no hidden API, no binder relay, and it keeps
  working while 雫 is stopped, because `system_server` persists the delegation. `PolicyApiClient`
  covers what **no scope can carry** (`setPermittedAccessibilityServices`, `addUserRestriction`,
  `setAlwaysOnVpnPackage`, `setCameraDisabled`, `setUserControlDisabledPackages`) via
  `ContentResolver.call` on 雫's provider, authority **`shiroikuma.shizuku.policy`**. **Prefer the
  delegated path wherever it reaches** — routing a delegatable call through 雫 trades a persistent
  grant for a live dependency. Method names on the wire (`status`, `set_accessibility_blocked`,
  `set_user_control_disabled`, `set_user_restriction`, `set_always_on_vpn`, `set_camera_disabled`,
  `clear_all_locks`) are shared with 雫 — **never rename one**.
- **Why a provider call and not a broadcast** (unlike the 保存復元 contract): it is **synchronous** and
  answers with a `Bundle`, which is what a switch the user just tapped needs; and
  `Binder.getCallingUid()` identifies us on the far side, so no shared token is needed — the token in
  保存復元 exists precisely because a broadcast has no trustworthy sender.
- **`setDelegatedScopes` can only be called by the owner app**; there is no `dpm` shell command for it
  (checked against `dpm help` on Android 13 and 15). 白い熊 authorises us in 雫 → Settings → **Feature
  Hub** → **Device policy powers** (a `CollapsiblePreferenceCategory`, **collapsed by default** like
  every category on that page — `android:defaultValue` is read as *defaultExpanded*) → the 白い熊 応用管理
  row → *Allow device-policy powers*. One switch does both halves: 雫's own policy allowlist **and**
  `setDelegatedScopes`.
- **LANDMINE — the CALLEE mints the job id, and the caller must adopt it (白い熊, +116).** Every sister app answers `export`/`import` with `OK:<its own job id>` (`AutomationJobs.begin()`) and broadcasts completion carrying **that** id; `AppDataClient` invented a UUID, sent it, and then filtered replies on it — so the reply arrived, failed the correlation, was dropped, and the transfer sat until the two-minute silence watchdog killed it. A 22 MB ArcaneChat export that had already finished on disk was reported as **"Could not backup"**, which reads as a broken door rather than a broken client. `transfer()` now adopts the returned id (still accepting its own, which costs nothing) and holds terminal replies that arrive **before `call()` returns** in an `unclaimed` map, claiming them the moment the id is known — without that the fix works for a slow export and fails for a fast one.
- **LANDMINE — `setPermissionGrantState` accepts dangerous RUNTIME permissions and nothing else
  (measured 2026-08-01).** Having *a* permission is not enough: `GET_USAGE_STATS` is backed by
  `android.permission.PACKAGE_USAGE_STATS`, protection `signature|privileged|development|appop|
  retailDemo`, so a lock offered on *Read app usage* was accepted by our UI and thrown away by the
  platform. `AppDetailsSnoopingItem.policyLockable` is resolved from the protection level up front
  (`PROTECTION_DANGEROUS`) **and** requires the app to actually request the permission — the same
  discipline `SnoopingResolver#canWritePermission` applies to the ordinary write path. App-op-only
  rows can never be locked and show no padlock at all.
- **Read the lock from the platform, never from our own store.** `isPermissionLocked` asks
  `getPermissionGrantState`; a lock set by 雫 directly, or left by an older build, shows the padlock
  just the same. Every write re-reads afterwards and reports whether it landed;
  `setPackagesSuspended`'s return array is the packages it could **not** touch, so a non-empty return
  is a failure, not a success with a note.
- **LANDMINE — refreshing the card is not refreshing the row.** `loadPolicyState` ends in
  `notifyPolicyChanged`, which invalidates **row 0 only** (the policy card); a row's `policyLocked` is
  filled in by `SnoopingResolver`, i.e. only on a full reload. A lock landed, the toast said
  "Applied", and the padlock kept drawing its stale hollow self. Row-level writes go through
  `runRowPolicy`, which calls `refreshPolicyLockState(packageName)` (the cheap half, needing only the
  package name) and rebinds that row.
- **UI.** A policy card is pinned above the capabilities (`item_app_details_snooping_policy.xml`,
  adapter `TYPE_POLICY` at index 0 — added **only when there is at least one capability row**, because
  `AppDetailsFragment` drives the empty view off the item count). It states whether the powers are
  live and, when not, *why* — no Device Owner on this phone versus authorised in 雫 but not for us —
  carries the **suspend** switch (a harder freeze than hiding) and a pill into *More device-policy
  controls…*. Each capability row gets a **padlock column left of the toggle**, hollow where a lock
  could land and filled where one does, yellow in both states; **tap locks, tap again releases, no
  confirmation either way** (白い熊: the explanation belongs in the legend, not in a dialog dismissed
  every time).
- **`DangerDialog` has two shapes, and the distinction is deliberate.** `confirm` — 危険 heading,
  **Cancel in the positive slot**, destructive choice red in the quiet negative slot — is reserved for
  powers that can leave the phone hard to operate or recover (accessibility blocking, user
  restrictions, always-on VPN lockdown). `confirmReversible` — no heading, normal button placement —
  is for what this page releases in one tap. Calling suspension 危険 was simply wrong: it is undone by
  the same switch that applied it.
- **The way back must always exist.** `clear_all_locks` lives in 雫 because it must work when 応用管理
  is gone, and because the Device Owner can undo what its delegate did by walking a package's
  permissions for anything not at the default grant state — no ledger needed on either side. Our
  fallback releases what this page can see (its own locked rows, suspension, uninstall block) and
  reports honestly when the remote half did not answer.
- **The legend is the documentation.** ⋮ → *What the marks mean* explains the card frame, the remember
  box, the padlock, the status pill and what locking actually does. Add to it rather than inventing a
  new explanatory surface.
- **Remember/forget is NOT a device-policy matter** and no longer sits in that menu. It is the row's
  **long-press**, shown as a small rounded box around the switch (`rememberedBox`, built in code so it
  takes `ForkThemeUtils.getTextColor()`). `AppDetailsViewModel.rememberSnoopingSetting` deliberately
  stores a state equal to the platform default — the "store only departures" rule governs writes made
  through the switch, but here the user asked explicitly, and pinning a default is how you say *keep it
  this way even if the default changes*.

## Battery history (`battery/`)

Fork screen **Battery history / 電池** — per-app drain over time, entered from the battery icon on the main top bar (right of the monitor gauge). Answers what the monitor cannot: *what drained the battery while you weren't looking*. Added +24 after a real investigation (2026-07-29) that found `shiroikuma.jami` sustaining **153 packets/second backgrounded** (~98 MB/h) and, as a result, **deep Doze never engaging once** — only `Device light idling` ever appeared in the dump.

- **The permission is the whole design.** `android.permission.BATTERY_STATS` is `signature|privileged|**development**` (verified on-device 2026-07-29 — same level as `DUMP`), so `SelfPermissions.init()` grants it to us exactly as it already grants `DUMP`. The platform **persists** that grant, so once it lands the sampler reads counters **in-process and survives reboot with no shell alive** — which is what makes unattended sampling viable on a phone whose ADB-over-TCP dies at every reboot. `BatteryStatsReader` tries the in-process binder dump first (`ServiceManager.getService("batterystats")` → `binder.dump(pipe, {"--charged"})`, read on a **separate thread** or the 64 KB pipe deadlocks), falling back to `Runner` only for the window before the grant.
- **LANDMINE — three different CPU numbers, only one is window-scoped.** `Proc <name>: CPU: … usr + … krn` accumulates over the **process lifetime** (a long-lived process shows minutes of CPU inside a 4-minute window — this produced a wrong "19% of a core" answer once); `Total cpu time: u= s=` is the window-scoped field and is what `BatteryStatsParser` reads (barely populated on this device, so the network/wakelock counters carry the ranking). Always establish a rate by **diffing two dumps**, never by reading one.
- **LANDMINE — `--checkin` is NOT the same data.** Its `nt` network counters are **lifetime** totals, reading 5–6× the "Statistics since last charge" section for the same uid. Never mix the two sources. `--checkin` remains the right place for the uid→package map (`9,0,i,uid,<uid>,<pkg>`).
- **LANDMINE — duration regex.** Durations print as `1h 14m 1s 69ms`; a `(\d+)(h|m|s|ms)` alternation matches `69ms` as 69 **minutes**. `ms` must come first. This cost a wrong answer.
- **No mAh anywhere, deliberately.** Huawei's power profile is gutted — the dump reads `Capacity: 5.00`, `Computed drain: 0`, `Estimated battery capacity: 1.00 mAh` — so `BatteryStatsManager`'s per-app mAh is fiction here. The ranking is a composite **"impact"** score (`BatterySampleDao.BatteryAggregate.impactScore()`) over measured counters: wakelock ms + radio-active ms + 1.5× CPU ms + 0.5× sensor ms + 2 ms/packet + 0.1× fg-service ms. Rows ≥25% share render blood red.
- **Storage is deltas, never absolutes.** The platform zeroes every per-uid counter at full charge, so `BatterySampler` diffs consecutive `BatterySnapshot`s and stores the difference (`db/entity/BatterySample`, Room **v8**, migration `M_7_8` — its CREATE TABLE must stay byte-identical to `AppsDb_Impl`'s or the DB refuses to open and takes the whole app with it). Reset detection is `BatterySnapshot.continues()` = same `Start clock time` **and** non-decreasing `Time on battery`; on a reset the absolutes *are* the post-reset usage and get written as one bucket rather than dropped, because the hours around a charge are the ones worth seeing. Per-counter deltas clamp at zero (a dying process shrinks some counters without a full reset). A dump that fails to parse writes **nothing** — an all-zero bucket would read back as a quiet hour that never happened.
- **Sampling is free by construction.** `BatterySamplerJob` is a `setPersisted` periodic `JobScheduler` job (15 min default = JobScheduler's own floor), **no wakelock, no foreground service, no exact alarm** — the counters are cumulative, so a late reading only widens a bucket. `--charged` cuts the dump from 443 KB to 69 KB. A monitor that woke the phone to measure why the phone was awake would be its own worst finding.
- **mAh where it's real, never where it isn't (+25).** The parser now reads the `Estimated power use (mAh)` block: `Capacity: X, Computed drain: Y` plus per-uid `UID u0a954: 394 ( screen=… cpu=… wifi=… )`. **The sanity gate is `Capacity >= 100`** — no phone has a sub-100 mAh battery, so a smaller value means `power_profile.xml` is stubbed (Mate XT: **5.00**). `powerModelUsable` is stored **per bucket**, not assumed globally, so a history spanning several phones (or a ROM update that fixes the profile) can never make old zero-mAh rows read as genuine "used no power". Where usable, the row headline becomes mAh and a "Estimated power (mAh)" sort appears; where not, the sort isn't offered at all and the share-of-impact percentage stands in. ⋮ → *Status* always states which case you're in and why.
- **Open-ended metrics, because it runs on several phones (+25).** Beyond the fixed columns, each bucket carries an `extras` JSON map: per-component mAh (`mah.cpu`, `mah.wifi`, …), job/sync time + counts, process-state times (top/foreground/background/cached), Wi-Fi running/lock/scan, Bluetooth scan, audio/video/camera/flashlight/vibrator, radio wakeup count. Simple `Label: <duration>` counters are **table-driven** (`DURATION_METRICS` in `BatteryStatsParser` — adding one is a single row), and `BatteryMetrics` renders **unknown keys too** (de-underscored, with the unit inferred from an `_ms`/`_count`/`mah.` suffix), so a counter a future Android release starts printing still shows up instead of being dropped. Component sets are never enumerated — they differ per release.
- Prefs: `shiroikuma_battery` (decisions — travels in export, classified `MONITOR`) and `shiroikuma_battery_state` (the previous raw snapshot — in `EXCLUDED_PREFS`, since restoring another phone's counters makes the next delta garbage). Scheduling is kicked from `Ops.setAuthenticated`, `BootReceiver`, and screen open.
- **Control panel, not a dialog (+26).** Tapping a row opens `battery/BatteryAppDetailActivity` — header (tap → App details, snowflake toggles freeze) · **Recommended** · History bars · Controls · Measurements. Long-press keeps the old raw-counter dialog as the quick look. `battery/BatteryAdvisor.java` is the point of the screen: it maps the *dominant counter* to the lever that addresses it (packets → network lever, wakelock ms → `WAKE_LOCK` op, CPU → `RUN_ANY_IN_BACKGROUND`, sensor ms → location, fg-service → `START_FOREGROUND`, holding a Doze exemption while busy → drop it), and **freeze is offered last and only after something already scored HIGH** — a map app burning GPS needs a location lever, not removal. All thresholds are **rates, not totals**, so an app measured over 4 minutes and one over 4 days are judged alike. `battery/BatteryControls.java` is the thin facade over `FreezeUtils` / `PackageManagerCompat` / `SnoopingLevers` / app ops — never grow a second copy of those rules here; in particular freeze goes through `FreezeUtils.freeze`, so the **必要 guard keeps working** and its throw is surfaced as `protected_profile_block`. `setOpAllowed` **re-reads the op after writing** and reports whether the platform complied (`AppOpsService` accepts writes it discards).
- **LANDMINE — frozen apps need the match flags.** A package frozen by *hiding* is invisible to a plain `getApplicationInfo`, so `BatteryUsageViewModel.resolveInfo` and the panel's `gather()` both pass `MATCH_UNINSTALLED_PACKAGES | MATCH_DISABLED_COMPONENTS`. Without them the very apps you froze for draining lose their label and icon and render as a bare package string. Rows show frozen state like the main list (icon at 0.5 alpha + filled ice-blue `ic_snowflake_24dp`), the icon column toggles freeze, and a ✕ force-stops — hidden for frozen rows and package-less uids, where it means nothing.
- **Alerts, comparison, screen-off view (+27).** **Alerts** (`battery/BatteryAlerts.java`, **off by default**) run from `BatterySampler.sample` — the point of background sampling is that the drain happens in a pocket, so the "went rogue" check cannot live on a screen you must remember to open. Judged over a **trailing hour** (`aggregateRange`), never a single bucket, and skipped unless ≥half that hour is actually covered — otherwise the first bucket after a gap is judged as if it were the whole window. Thresholds: packets/s, wakelock % of hour, CPU % of a core; 6 h per-uid cooldown stored in the **state** prefs (device-local: an exported "already warned" would mute the alert on another phone), plus a per-app ignore list for apps you accept as heavy. The notification opens straight into that app's control panel. **Comparison**: `aggregateForUidRange` sums the equally long window immediately before, and the panel compares **rates, not totals** — the earlier window is often shorter (history doesn't reach back yet) and comparing sums would invent an improvement that is only missing data; it is skipped entirely when the previous window is under a quarter as long. Green = down, because every metric here is a cost. **Screen-off view**: `aggregateScreenOff` restricts to buckets whose device row has `screen_on_ms = 0` — every row in one sampling pass shares a `ts`, so the device row is the authority for all of them. Strict on purpose: a bucket where the screen came on at all is contaminated and dropped whole. The toolbar subtitle says when the filter is on, or the smaller numbers read as a bug.
- **Battery list uses the main list's conventions (+30).** `item_battery_card.xml` mirrors `item_main.xml` — `MaterialCardView` + film, icon column carrying the freeze snowflake and the force-stop ✕ under the icon, italic label for frozen rows, type-coloured running box (user yellow / system orange) — and only the right-hand column differs: version/backup/signature is replaced by share-of-drain + bar + the counters behind it. `BatteryUsageAdapter` **shares the conventions, not the code** (the main adapter is bound to `ApplicationItem` and the selection/profile machinery), but reads every colour from the same `ColorPrefs`/`RunningBoxPrefs` keys, so the two screens cannot drift apart; `reloadColors()` runs from `onResume`. Column layout via `BatteryLayoutPrefs` + the same 3×3 toolbar icon. **Both grid icons are `showAsAction="always"`** — as `ifRoom` they silently fell into the overflow on a narrow panel, which is where 白い熊 kept finding the main one.
- **Battery screen header (+30).** Fixed panel above the list, about two rows tall: left `BatteryLevelView` (device level trace, voltage as a fainter second trace scaled to its own visible range, **charging stretches as a red band** — a discharge chart that silently merges charging shows a line going *up* and the reader stops trusting all of it), opening on the last 24 h with the full retained history reachable by dragging left, pinch-zoom + double-tap-fit; right `BatteryTopDrainersView` (last **2 h**, up to 5 colour columns with percentages — "what is eating it right now" is a different question from "what ate it today"). Needs `voltage_mv` + `charging` on the device row (Room **v10**, `M_9_10`), read from the sticky `ACTION_BATTERY_CHANGED` — voltage has no `BATTERY_PROPERTY_*` constant and the sticky intent needs no privileges.
- **History chart is a real time axis, not an index axis (+29).** `battery/BatteryHistoryView.java` places and *sizes* each bar by its own `[ts - duration, ts)`. **This is the point of it:** buckets are not uniform — Doze defers the sampler, so an overnight bucket can be hours wide while a daytime one is 15 minutes — and an index-based chart draws those as equals, erasing exactly the idle gaps you are investigating. Pinch zooms (focus-preserving), drag pans, double-tap fits, tap selects a bar and the caption becomes the readout (its real window + bucket length). The vertical scale is the max of the **visible** bars, so zooming into a quiet stretch reveals its shape instead of leaving it flat under one distant spike. Day boundaries draw as brighter delimiters with date labels; a finer tick ladder (1m…1d, chosen for ≤8 ticks) draws underneath with clock labels, all aligned to *local* wall-clock via `Calendar`. **LANDMINE — the chart lives inside a `NestedScrollView`:** without `getParent().requestDisallowInterceptTouchEvent(true)` on ACTION_DOWN the first vertical wobble of a pan hands the gesture to the scroller and the chart never sees the rest of it; and the scroll detector must be skipped while `ScaleGestureDetector.isInProgress()` or pinch and pan fight.
- **LANDMINE — `SnoopingLever.stateLabelRes` returns 0 meaning "use the generic label", not "no label".** Passing it straight to `getString` throws `Resources$NotFoundException: String resource ID #0x0` — this crashed the whole control panel in +27 (the network lever names only its FOREGROUND rung, so ALLOWED/BLOCKED returned 0). Any caller must mirror `AppDetailsSnoopingFragment`'s fallback to `snooping_state_allowed`/`_blocked`/`_foreground`; `BatteryAppDetailActivity.stateLabel()` does. **Build-pipeline lesson from the same fix:** the hard gate only checks that the server JARs are non-empty, so when the Gradle daemon died mid-build the script happily signed the *previous* build's leftover unsigned APK under the new version name. **Always confirm `BUILD SUCCESSFUL` before signing**, and if in doubt compare the compiled `.class` mtime against the edited source.
- **Room migrations here are load-bearing.** `M_7_8` creates the table, `M_8_9` adds `power_mah`/`power_model_usable`/`extras`. Room compares **column defaults** as well as types, so `@ColumnInfo(defaultValue = "0")` on the entity and `DEFAULT 0` in the `ALTER` must agree exactly — verify against `AppsDb_Impl.java`'s generated `createAllTables` + `_columnsBatterySample` after any change. A mismatch throws at DB open and takes the **whole app** down, not just this screen.
- **LANDMINE — the fork and upstream both spent schema v8, and the fork keeps it (白い熊, 4.1.1 sync).** Upstream 4.1.1 introduced its own `M_7_8` creating `permission_override` (the controller-based permission system) and shipped its own `8.json`. This fork's v8/v9/v10 were **already spent** on the battery sampler and are **live on the phone**, so upstream's table was re-numbered into a new **`M_10_11`** and the database is now **version 11**; `8.json` stays the fork's battery schema. **Never renumber the battery migrations to make room for an upstream one** — an installed device sitting at v10 would either lose its history or refuse to open, and a refusal at DB open takes the whole app down. The rule generalises: when upstream adds a migration whose number this fork has already used, the upstream table is appended at the fork's next free version, never inserted.

### Doze on the Mate XT (measured 2026-07-29)

`dumpsys deviceidle` reports `mDeepEnabled=true`, `mLightEnabled=true` — **AOSP Doze is fully enabled**, and it is *not* PowerGenie (Huawei's own freezer, which 白い熊 has disabled; the two are unrelated mechanisms). Timings are stock: `inactive_to=30m`, `idle_after_inactive_to=30m`, `sensing_to=4m`, `locating_to=30s` — so deep idle needs roughly an hour of **uninterrupted** screen-off before it can engage, and any screen-on resets the walk. **`mUseMotionSensor=true` but `mMotionSensor=null`** — the device exposes accelerometers and a step detector but **no wake-up significant-motion sensor**, so the controller never binds one; whether that blocks deep idle or merely defers it to the 4-minute `sensing_to` timeout is not answerable from a dump. **Do not conclude "deep Doze never engages here" from a short window** — a 74-minute cycle with 27 minutes of screen-on could never have reached it regardless, and an earlier session wrongly called its absence a smoking gun. The battery screen records `Device full idling` per bucket, so an overnight run answers it properly; `dumpsys deviceidle force-idle` (screen off, then `unforce`) settles it immediately.

## Process monitor / reaper (`processreaper/`)

Fork replacement for the legacy "Running apps" screen, now **deleted** (`RunningAppsActivity`/`Adapter`/`ViewModel`/`RunningAppDetails`, their layouts/menus, the manifest + `activity_main_actions` entries, and `Prefs.RunningApps`). The **shared** enumeration infra it relied on stays — `runningapps/ProcessParser`, `ProcessItem`, `AppProcessItem` (the monitor uses these). The two now-dead `PREF_RUNNING_APPS_*` keys are kept in the `AppPref` enum with inlined int defaults (0) to avoid touching the persisted enum's ordering. Entry point: the standalone gauge icon (`drawable/ic_monitor.xml`, custom `actionLayout` `view_toolbar_monitor.xml`) left of the filter icon on the main top bar; long-pressing the monitor's overflow opens the 白い熊 応用管理 UI page (same `attachOverflowLongPress` trick as MainActivity).

- **Enumeration** reuses `runningapps/ProcessParser` (made callable via the new `public static getRunningProcessList()`). Live **instantaneous CPU%** is NOT from `ProcessItem` — its `cpuTimeConsumed` is truncated to whole seconds (`Ps.java` divides ticks by CLK_TCK). The monitor samples raw `utime+stime` ticks itself via `Runner.runCommand("cat /proc/[0-9]*/stat …")` and diffs across refreshes (`ProcessMonitorViewModel.sampleCpuTicks`). **Memory ranks by PSS, not RSS** (`samplePss`): RSS double-counts shared pages, but killing a process frees roughly its *unique* footprint = PSS, so PSS is the right number for a reaper. **LANDMINE:** non-root shell **cannot** read `/proc/<pid>/smaps_rollup` (ptrace-gated → `EPERM` cross-process; only ~8 of ~787 pids, all shell-owned, are readable) — verified on-device, so the cheap `/proc` path is dead. The only source is `dumpsys meminfo`'s "Total PSS by process" section (`<kB>K: name (pid N …)`), parsed pid→bytes and **cached `PSS_TTL_MS` (15 s)** because that dumpsys is heavy and memory moves slowly (vs. the 2 s CPU tick). `memBytesFor(p)` returns PSS when known, else falls back to `p.getMemory()` (RSS) — used for both the displayed size and the sort key in all three row builders (`singleRow`/`sharedRow`/`leakRow`).
- **`ProcessClassifier`** = PROTECTED vs KILLABLE. **LANDMINE:** `ProcessParser` resolves uid 2000 to its owning package `com.android.shell`, so every shell process (incl. orphaned `logcat`) arrives as an `AppProcessItem` — gate leak/shell logic on the **kill method** (`METHOD_SIGKILL` = shell-owned), NEVER on `instanceof AppProcessItem`. Smart kill router: app package → `PackageManagerCompat.forceStopPackage`; package-less shell pid → `kill -9 <pid>` (shell can signal its own uid; the legacy screen wrongly gated this on root). Built-in denylist (Tasker/Shizuku/self/IME/system…) + **user-editable** protected set (`ReaperPrefs`, tap-Protect/Allow); the privilege chain (our own `am.jar`/`main.jar` server, Shizuku) is hard-protected so the reaper can't sever its own kill ability. The **built-in denylist is itself user-overridable** (`ReaperPrefs` `allowed_packages`): **long-press** a built-in-protected app (e.g. Huawei Home) → "Allow killing" makes it killable; long-press again → "Restore protection". `ProcessClassifier.isOverridableDenylist(pkg)` (= in `DENYLIST` && NOT a `SHELL_PROTECT_SUBSTR` match) gates this — the privilege chain (shizuku/am/self) can never be overridden. The adapter routes long-press to the override toggle for these apps (instead of multi-select) and hides their Protect button; `classify`'s denylist check skips protection when the pkg is in the allowed set and not privilege-chain. The foreground launcher still shows "in use" while it's on screen, so kill it from inside the monitor (launcher backgrounded). **LANDMINE — our own command shells:** `Runner` pipes every command (including the kill) through a long-lived `sh` spawned by the `:priv:0` server, plus transient `sh -c` helpers per command (each refresh runs several dumpsys/cat). These arrive as bare `sh` tagged `com.android.shell` (uid 2000) — the cmdline-substring guard misses them, so they were mis-grouped as a killable **"sh ×N" leak**; tapping it tried to `kill -9` the very shell running the kill and failed with "Couldn't kill". Fixed by three layers: (1) **ancestry protection** (`ProcessClassifier.ancestryProtectedPids`) walks each pid's `PPid` chain (harvested for free from the same `/proc/*/stat` batch — field 1 after `comm` — into `mPpid`) and protects (`"privilege"`) anything descending from our roots; genuine orphaned leaks (reparented to init) are NOT descendants and stay killable. (2) **LANDMINE — root detection can't use `ProcessParser`:** the `:priv:0` server runs as **uid 2000** with **`comm` = "main"**, is **reparented to init**, and `ProcessParser`'s name/cmdline fields don't expose our package for it — so the server was never flagged as a root and the walk `sh → main(:priv:0) → init` hit nothing. Roots are read straight from `/proc/*/cmdline` via `grepOurRootPids()` (`grep -la -e <pkg> -e shizuku -e am_local_server /proc/[0-9]*/cmdline`). **The chain is INVERTED from the obvious guess:** the ADB-mode tree is `am_local_server (uid 2000, comm "main", reparented to init) → sh → sh → shiroikuma.oyokanri:priv:0`, i.e. the package-bearing `:priv:0` is the **leaf** and the two helper `sh` are its **ancestors** — so a pure descendant-closure from the `:priv:0` root never reaches them. Two cover it: (a) the grep also matches `am_local_server` (the true top of the chain) so its whole subtree closes downward; (b) `ancestryProtectedPids` also walks **UP** via PPid from each **uid-2000** root, adding ancestors until init (pid ≤ 1) — never from the app / shizuku-api (uid ≥ 10000, zygote children — walking up would hit the shared zygote and over-protect everything). (3) **Liveness filter:** the monitor's own pipeline (`ps`, `cat`, `sed`, `sh` — all names in `TRANSIENT`) spawns helpers that land in the enumeration then die ms later, masquerading as a leak. `load()` now enumerates FIRST, reads `/proc/*/stat` AFTER, and drops any pid absent from that later stat read (ghosts gone; the persistent server shell that survives is caught by ancestry). Also, SIGKILL **reports by whether the kill was permitted, not by exit code or a liveness probe** (`doKill`): `kill -9` returns non-zero if any listed pid already exited (ESRCH), and — the subtler trap — it returns *before* the kernel finishes teardown, so an immediate `[ -d /proc/$p ]` race-catches a dying/zombie entry and reports a false "Couldn't kill" even though the row vanished. For a same-uid SIGKILL the only true failure is **EPERM**, so `doKill` runs `kill -9 <pids> 2>&1` and fails only if the output contains "operation not permitted"/"permission denied"; ESRCH ("No such process") and silent success both count as killed. **Actively-in-use protection** (reason `"in use"`): the foreground/top app(s) and any process hosting a live **foreground service** (media/nav/recorder/sync) are protected so you can't one-tap kill what's on screen or playing. Detected in `ProcessMonitorViewModel.gatherActivePackages()` by parsing `dumpsys activity services` (ServiceRecord blocks carrying `isForeground=true`) and `dumpsys activity activities` (every `ResumedActivity` line — plural on the foldable in split-screen), unioned into a package set passed to `classify(...)`. The set is **cached `ACTIVE_TTL_MS` (5 s)** because the two dumpsys parses are heavier than the per-tick `/proc` read and foreground state changes slowly; best-effort, so a parse miss just drops back to the prior (less-protective) behaviour, never a crash. These rows render grey (`MONITOR_PROTECTED`) with no Kill/Protect button — no adapter change was needed.
- **Leak grouping** clusters identical package-less shell commands into one "comm ×N — LEAK" row (multi-pid `kill -9`); leaks are **pinned to the top** of the sort (they're tiny and would otherwise sink). The cluster size is the **configurable** `MonitorPrefs.getLeakThreshold` (default 3, min 2, max 10; known-transient comms in `TRANSIENT` floor at 2). An optional **min-age** filter (`getLeakMinAgeSec`, default 0 = off) only flags a cluster once its **oldest** member has lived that long — so a momentary burst isn't flagged; age is `elapsedRealtime − starttime/CLK_TCK`, with `starttime` (proc stat field 22 → `mStart`) harvested from the same `/proc/*/stat` batch as ppid. Shared processes (one pid, many packages, e.g. `system_server`) dedupe into one row. **Multi-process apps collapse by package** (`appRow`): a browser's main + `:tab`/`:gpu` children (same package, uid ≥ 10000, not shell) become ONE row summing RAM/CPU with all pids — force-stop kills the whole package anyway, so N near-identical rows were just noise (`isAppGroupable` gates it; single-process apps still go through `singleRow`). Toolbar subtitle shows "N processes · M killable · L leaks". A **toolbar SearchView** (`action_monitor_search`, themed yellow-on-black) filters the live list by app label / process name / package (`ProcessMonitorViewModel.setQuery` → `filterRows` over the cached `mAllRows`); the query survives the 2 s auto-refresh because every load re-applies it. (Search is the way to find a specific app like the launcher, which sinks low in the PSS-sorted list.) **Tap a row opens the dedicated `ProcessDetailActivity`** (a full screen, not the kill dialog — kill is the per-row ✕ button or the detail's "Kill" toolbar action). It's launched with the cheap fields as Intent extras (pid, pids[], pkg, title, killable, method, uid, reason, user, cpu, pss, in-use) and gathers the rest fresh for its one pid via a single privileged read of `/proc/<pid>/{status,stat,oom_score_adj,oom_score,attr/current,cgroup,cmdline,fd}` (marker-delimited `@ST@`/`@SS@`/… sections, parsed in `gather()`), plus app metadata from the PackageManager. Sections: Process · Memory (PSS/RSS/peak/virtual/data/stack/swap/FDs) · CPU & scheduling (CPU%/CPU time/priority/nice/policy/ctxt switches) · Lifecycle & security (oom_score_adj/oom_score/in-use/age/tracer/seccomp/SELinux/cgroup) · App (label/version/SDKs/system/debuggable/install dates/paths) · Classification. Values render as a two-column label→value list, **selectable** (copyable). A tappable **app icon header** sits at the top (`addIconHeader`, `ImageLoader`) — icon on the left with the app label (large) + package id (smaller) stacked to its right; tap the header to jump into `AppDetailsActivity`. Toolbar: App details (→ `AppDetailsActivity`, real apps only), Kill, Refresh. Must be in `AppearanceUtils.isYellowOnBlackActivity()` + a manifest `<activity>` with `AppTheme.YellowOnBlack`. Long-press still does override/multi-select; the per-row buttons still Protect/Kill. **The whole detail page is settable** in the 白い熊 応用管理 UI "Process detail page" group: header **icon size** + **line padding** sliders (`MonitorPrefs.getDetailIconDp`/`getDetailRowPadDp`), plus the five text surfaces (app label, app ID, section header, field label, field value) each with full font attrs via the data-driven `FontPrefs`/`FontUtil.apply` system (family + weight + size) + colour (`ColorPrefs.MONITOR_DETAIL_*`). `ProcessDetailActivity` sets factory defaults then `FontUtil.apply(tv, FontPrefs.MONITOR_DETAIL_*)` (size 0/weight 0/family "" = inherit, so DEFAULT cascades). A **faceted filter** (toolbar funnel `action_monitor_filter`) narrows the list across two AND-ed dimensions — killability {Killable, Protected, **Togglable**} × type {User, System, Shell, Leaks} (`ProcessMonitorViewModel.setFilter(killMask, typeMask)` + `typeOf(row)` + `killBits(row)`; combined with the search query in `filterRows`). The killability facets **OR-combine** (`killBits` is a bitmask): a **Togglable** row is protected *now* but user-flippable to killable (`isTogglable` = reason `"you"` OR `isOverridableDenylist`), and it also carries `F_PROTECTED`, so **Killable + Togglable = everything that can ultimately be killed**. Session-only multi-choice dialog; nothing checked in a dimension = unconstrained.
- **Prefs / UI** (all in the `pref_monitor_*` "Process monitor" group on the UI page; live via per-bind reads + `onResume` separator reload): columns — adaptive (default, the 450dp auto-fit grid) or a fixed 1–4, held in `MonitorPrefs` and picked from the **same single-choice dialog as the main list** (`ProcessMonitorActivity.showLayoutPicker`/`applyListLayout`, mirroring `MainActivity`; the top-bar grid icon used to cycle blind, which showed neither the options nor the current one) — app-icon size, row vertical padding, **leak threshold + leak min-age** sliders, four row-state colours (`ColorPrefs.MONITOR_KILLABLE/LEAK/PROTECTED/USER_PROTECTED`), and the separator grid (`MonitorSeparatorPrefs` + `ColorPrefs.MONITOR_SEPARATOR_H/V`, drawn by `MonitorSeparatorDecoration` — a copy of `MainSeparatorDecoration` that also draws a top line on the first row).
- **Dialogs LANDMINE:** `DynamicColors.applyToActivityIfAvailable` (in `AppearanceUtils`, run after `setTheme`) clobbers the activity's `materialAlertDialogTheme`, AND `MaterialAlertDialogBuilder.create()` overrides the window background with its own borderless `MaterialShapeDrawable`. So themed dialogs must (a) pass `R.style.ThemeOverlay_AppTheme_MaterialAlertDialog_YellowOnBlack` **explicitly** to the builder (for yellow text/radio), and (b) set the bordered drawable on the **window background after `show()`** (`presentWithYellowBorder`) — theme attrs alone won't paint the border. The yellow-on-black dialog overlay now also pins `colorControlActivated` (selected radio) and a 2dp border stroke. Any new fork screen must be added to `AppearanceUtils.isYellowOnBlackActivity()` or it gets the dynamic theme.

## Conventions

- Fork-only Java/XML additions get a comment beginning `// Fork: …` (or `<!-- Fork: … -->`) so they're recognisable later.
- New public Java files start with `// SPDX-License-Identifier: GPL-3.0-or-later`.
- The selection toolbar button label is whatever `MainToolbarPrefs.titleForKey` returns; the menu-XML title is overridden.
- For new toolbar actions: id in the selection-actions menu XML + key registered in `MainToolbarPrefs.ALL_KEYS` + entries in the three switch maps + dispatch in `MainActivity` + (optionally) gating in `MainBatchOpsHandler`.

## Workflow notes for Claude Code (vs. the earlier Claude.ai sessions)

The skill file `.claude/skills/appmanager-fork/SKILL.md` describes a fork-development history that ran inside Claude.ai with a paste-ready patch workflow ("emit a patch → user applies → user says Push → finalize hash"). **In Claude Code that workflow no longer applies** — you have direct file-edit, bash, and git tools. So:

- Edit files in the working tree directly. Don't generate patch files unless the user explicitly asks for one.
- Don't generate paste-ready shell blocks (cyan echo prefixes, yellow gates, etc.); run the commands yourself.
- "Build" still means run the full pipeline above. "Push" still triggers the commit-and-push flow. Note that you also build automatically after finishing any change (see **"Always build after changes"** under Build & deploy pipeline) — an explicit "Build" is just a way to force it again.
- The skill's historical "deliver patch + zip + in-flight bullet → swap to hash" rituals are claude.ai artefacts; just commit directly with a good message body.
- When a new piece of knowledge surfaces (a new chokepoint, a non-obvious file naming convention, a regression cause), update either this CLAUDE.md or the `appmanager-fork` skill so it persists into the next session.

## Commit / branch process

- `custom` is a growing stack of small, self-describing commits on top of upstream `master`. **Keep appending commits**; when a new upstream version is released, **rebase `custom` onto it**. There is no goal to squash the history down to a fixed number of commits — do not autosquash/collapse the stack.

## Commit convention — no Claude attribution

Do **not** add any `Co-Authored-By: Claude …` trailer — nor a "🤖 Generated with Claude Code" / Anthropic-attribution line — to commit messages or PR bodies in this repo. 白い熊 does not want Claude attribution in the history; this **overrides** the harness's default to append such a trailer. End commit messages at the last line of the body. (The existing history was scrubbed of these trailers on 2026-06-08; the global rule lives in `~/.claude/CLAUDE.md`.)
